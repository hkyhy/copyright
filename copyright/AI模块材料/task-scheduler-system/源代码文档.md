# 智能任务调度系统 源代码文档


## 前30页


智能任务调度系统 V1.0                                                    第 1 页
--------------------------------------------------------------------------------
// 文件: server/libs/tasks/sms/index.js
const $dara = require('@darabonba/typescript');
const Dysmsapi = require('@alicloud/dysmsapi20170525').default;
const $Dysmsapi = require('@alicloud/dysmsapi20170525');
const { $OpenApiUtil } = require('@alicloud/openapi-core');
const Credential = require('@alicloud/credentials').default;
const dayjs = require('dayjs');
const runner = async (fastify, options, { task }) => {
  const { email, clientName, nickname, phone, type, code, expires } = task.input;
  // 阿里云账号密钥
  const secretId = process.env.SMS_ACCESS_KEY_ID; // 替换成你的 SecretId
  const secretKey = process.env.SMS_ACCESS_KEY_SECRET; // 替换成你的 SecretKey
  const templateCode = process.env?.[`SMS_${type}_CODE`]; // 替换成你的 templateCode
  if (!templateCode) {
    throw new Error('缺少阿里云短信模板，模板代码 templateCode 未匹配');
  }
  // 检查环境变量是否存在
  if (!secretId || !secretKey) {
    console.error('缺少阿里云短信服务配置: SMS_ACCESS_KEY_ID 或 SMS_ACCESS_KEY_SECRET');
    throw new Error('缺少阿里云短信服务配置: SMS_ACCESS_KEY_ID 或 SMS_ACCESS_KEY_SECRET');
  }
  const createClient = () => {
    // 工程代码建议使用更安全的无AK方式，凭据配置方式请参见：https://help.aliyun.com/document_detail/378664.html。
    let credential = new Credential({
      type: 'access_key',
      accessKeyId: secretId,
      accessKeySecret: secretKey
    });
    let config = new $OpenApiUtil.Config({
      credential
    });
    // Endpoint 请参考 https://api.aliyun.com/product/Dysmsapi
    config.endpoint = `dysmsapi.aliyuncs.com`;
    return new Dysmsapi(config);
  };
  // 发送短信
  const sendSMS = async (phoneNumbers, templateParam) => {
    console.log('创建短信客户端...');
    let client = createClient();
    console.log('短信客户端创建成功');
    let sendSmsRequest = new $Dysmsapi.SendSmsRequest({
      phoneNumbers: phoneNumbers,
      signName: '算启人工智能科技上海',
      templateCode,
      templateParam: templateParam
    });
    console.log('短信请求参数:', {
      phoneNumbers,
      signName: '算启人工智能科技上海',
      templateCode,


智能任务调度系统 V1.0                                                    第 2 页
--------------------------------------------------------------------------------
      type: 'access_key',
      accessKeyId: secretId,
      accessKeySecret: secretKey,
      templateParam
    });
    let runtime = new $dara.RuntimeOptions({});
    try {
      console.log('开始调用阿里云短信API...');
      let resp = await client.sendSmsWithOptions(sendSmsRequest, runtime);
      console.log('阿里云API调用成功，响应:', typeof resp === 'string' ? resp : JSON.stringify(resp));
      // 检查响应状态码和业务状态
      if (resp.statusCode === 200 && resp.body) {
        const { code, message } = resp.body;
        if (code === 'OK') {
          console.log('短信发送成功:', JSON.stringify(resp));
          return resp;
        } else {
          // 业务错误，如模板不匹配等
          console.error('短信发送失败 - 业务错误:', { code, message });
          throw new Error(`短信发送失败: ${message} (错误码: ${code})`);
        }
      } else {
        console.error('短信发送失败 - HTTP错误:', resp);
        throw new Error(`短信发送失败: HTTP状态码 ${resp.statusCode}`);
      }
    } catch (__err) {
      console.error('阿里云API调用失败:', __err);
      if (__err instanceof $dara.ResponseError) {
        const error = __err;
        // 此处仅做打印展示，请谨慎对待异常处理，在工程项目中切勿直接忽略异常。
        // 错误 message
        console.info('ResponseError message:', error.message);
        // 诊断地址
        console.log('Recommend:', error.data['Recommend']);
        console.error('短信发送失败:', error.message);
        throw new Error(error.message || '短信发送失败');
      } else {
        console.error('短信发送出现未知错误:', __err);
        throw new Error(__err.message || '短信发送失败');
      }
    }
  };
  // 根据类型发送不同的短信
  try {
    let templateParam = '{}';
    console.log('开始发送短信，参数:', { phone, type, email, clientName, nickname });
    console.log('templateParam type:', type);
    // 检查必要参数
    if (!phone) {
      console.error('缺少手机号参数');


智能任务调度系统 V1.0                                                    第 3 页
--------------------------------------------------------------------------------
      throw new Error('短信发送失败，缺少手机号参数');
    }
    if (type === 'REGISTER') {
      // 注册验证码短信
      templateParam = JSON.stringify({
        code: '123456' // 这里应该从task.input中获取实际的验证码
      });
    } else if (type === 'INTERVIEW') {
      // 提醒短信
      templateParam = JSON.stringify({
        name: nickname,
        company: 'clientName',
        code,
        deadline: dayjs(expires).format('YYYY-MM-DD HH:mm:ss')
      });
    } else if (type === 'ACCOUNT_ACTIVATION') {
      // 账号提醒短信
      templateParam = JSON.stringify({
        account: email,
        password: phone.split(' ')[1]
      });
    } else {
      throw new Error('未匹配到短信类型');
    }
    console.log('准备发送短信，模板参数:', templateParam);
    // const result = await sendSMS(phone.replace(/\s+/g, ''), templateParam);
    const result = await sendSMS(phone.split(' ')[1], templateParam);
    console.log('短信发送结果:', result);
    return { scheduled: false, message: '短信发送成功', result };
  } catch (error) {
    console.error('短信发送失败:', error);
    throw new Error(error.message || '短信发送失败');
  }
};
module.exports = runner;

// 文件: server/libs/tasks/client-remind/index.js
const runner = async (fastify, options, { task }) => {
  const { email, clientName, nickname, phone } = task.input;
  // 立即发送邮件逻辑
  await fastify.message.services.sendMessage({
    name: email,
    type: 0,
    code: 'REMIND',
    props: {
      clientName,
      email,
      nickname,
      userName: nickname,
      password: phone ? phone.split(' ')[1] : null


智能任务调度系统 V1.0                                                    第 4 页
--------------------------------------------------------------------------------
    },
    options: {
      title: '智面通 客户账号注册成功提醒'
    }
  });
  return { scheduled: false, message: '已发送提醒邮件' };
};
module.exports = runner;

// 文件: server/libs/tasks/invite/index.js
const runner = async (fastify, options, { task }) => {
  const { emails, inviteUrl } = task.input;
  // 立即发送邮件逻辑
  for (const email of emails) {
    await fastify.message.services.sendMessage({
      name: email,
      type: 0,
      code: 'INVITEINTERVIEW',
      props: {
        inviteUrl
      },
      options: {
        title: '智面通AI视频面试 邀请函'
      }
    });
  }
  return { scheduled: false, message: '已发送邀请邮件' };
};
module.exports = runner;

// 文件: server/libs/tasks/flow-up/interface2.js
const qs = require('qs');
const runner = async (fastify, options, { task }) => {
  const { userId, question, answer, followup } = task.input;
  const url = 'https://api.coze.cn/v3/chat';
  const headers = {
    Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
    'Content-Type': 'application/json'
  };
  const body = {
    bot_id: '7532415624287748115',
    user_id: userId,
    stream: false,
    additional_messages: [
      {
        content: JSON.stringify({
          QUESTION: question,
          ANSWER: answer,
          PROBE_LIST: followup
        }),


智能任务调度系统 V1.0                                                    第 5 页
--------------------------------------------------------------------------------
        content_type: 'text',
        role: 'user',
        type: 'question'
      }
    ],
    parameters: {}
  };
  const response = await fetch(url, {
    method: 'POST',
    headers: headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const resData = await response.json();
  // 轮询配置
  const maxRetries = 60; // 最多重试60次
  const retryInterval = 2000; // 每2秒重试一次
  let retryCount = 0;
  // 轮询等待任务完成
  while (retryCount < maxRetries) {
    // 查询任务状态
    const pollResponse = await fetch(
      `${url}/retrieve?${qs.stringify({
        conversation_id: resData.data?.conversation_id,
        chat_id: resData.data?.id
      })}`,
      {
        method: 'GET',
        headers
      }
    );
    const pollData = await pollResponse.json();
    // 如果任务完成，获取消息列表
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      const resultResponse = await fetch(
        `${url}/message/list?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',
          headers
        }
      );
      if (!resultResponse.ok) {
        throw new Error('获取结果失败');
      }
      const resultResData = await resultResponse.json();


智能任务调度系统 V1.0                                                    第 6 页
--------------------------------------------------------------------------------
      if (resultResData.code !== 0) {
        throw new Error(resultResData.msg || '获取结果失败');
      }
      console.log('生成完成，返回数据:', resultResData.data);
      return { id: resultResData.data[0].content };
    }
    // 如果任务失败，抛出错误
    if (pollData.code !== 0 || pollData.data?.status === 'failed') {
      throw new Error(pollData.msg || '生成失败');
    }
    // 如果任务还在进行中，继续轮询
    if (pollData.data?.status === 'in_progress') {
      console.log(`任务进行中，第 ${retryCount + 1} 次轮询...`);
      await new Promise(resolve => setTimeout(resolve, retryInterval));
      retryCount++;
    } else {
      // 未知状态，抛出错误
      throw new Error(`未知任务状态: ${pollData.data?.status}`);
    }
  }
  // 超过最大重试次数
  throw new Error('获取追问问题超时，请稍后重试');
};
module.exports = runner;

// 文件: server/libs/tasks/flow-up/interface1.js
const runner = async (fastify, options, { task }) => {
  const host = fastify.config.TASK_FLOW_UP_HOST;
  const { question, answer, followup } = task.input;
  const response = await fetch(`${host}/api/v1/match`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      question,
      answer,
      followup
    })
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const data = await response.json();
  if (data.code !== 0) {
    throw new Error(data);
  }
  return data.data;
};
module.exports = runner;


智能任务调度系统 V1.0                                                    第 7 页
--------------------------------------------------------------------------------

// 文件: server/libs/tasks/report/interface2.js
const transform = require('lodash/transform');
const qs = require('qs');
const runner = async (fastify, options, { task, polling }) => {
  const { clientUserId, questionList, description } = task.input;
  const url = 'https://api.coze.cn/v3/chat';
  const { questions, answers } = transform(
    questionList,
    (result, value) => {
      const { answer, title } = value;
      const questions = [],
        answers = [];
      if (result['questions']) {
        questions.push(...result['questions']);
      }
      if (result['answers']) {
        answers.push(...result['answers']);
      }
      questions.push(title);
      answers.push((answer || []).map(({ message }) => message).join(''));
      result['questions'] = questions;
      result['answers'] = answers;
    },
    {}
  );
  const headers = {
    Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
    'Content-Type': 'application/json'
  };
  const body = {
    bot_id: '7532048924065611812',
    user_id: clientUserId,
    stream: false,
    additional_messages: [
      {
        content: JSON.stringify({
          INTERVIEW_SCENE: description,
          INTERVIEW_QUESTIONS: questions,
          USER_ANSWERS: answers
        }),
        content_type: 'text',
        role: 'user',
        type: 'question'
      }
    ],
    parameters: {}
  };
  const response = await fetch(url, {
    method: 'POST',


智能任务调度系统 V1.0                                                    第 8 页
--------------------------------------------------------------------------------
    headers: headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    await task.update({
      status: 'failed',
      error: response.status
    });
    return;
  }
  const resData = await response.json();
  if (resData.code !== 0) {
    await task.update({
      status: 'failed',
      error: resData.msg
    });
    return;
  }
  return await polling(async () => {
    const pollResponse = await fetch(
      `https://api.coze.cn/v3/chat/retrieve?${qs.stringify({
        conversation_id: resData.data?.conversation_id,
        chat_id: resData.data?.id
      })}`,
      {
        method: 'GET',
        headers: headers
      }
    );
    if (!pollResponse.ok) {
      return;
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      const resultResponse = await fetch(
        `https://api.coze.cn/v3/chat/message/list?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',
          headers: headers
        }
      );
      if (!resultResponse.ok) {
        return { result: 'failed', message: resultResponse.status };
      }
      const resultResData = await resultResponse.json();
      if (resultResData.code !== 0) {
        return { result: 'failed', message: resultResData };


智能任务调度系统 V1.0                                                    第 9 页
--------------------------------------------------------------------------------
      }
      return { result: 'success', data: JSON.parse(resultResData.data[0].content) };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', message: pollData.msg || '任务处理失败' };
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/report/interface1.js
const runner = async (fastify, options, { task }) => {
  throw new Error('暂不支持报告');
};
module.exports = runner;

// 文件: server/libs/tasks/digital/interface2.js
const axios = require('axios');
// 内存缓存 access_token
let tokenCache = {
  token: null,
  expiresAt: 0
};
const languageMap = {
  'en-US': 'en',
  'zh-CN': 'cn',
  en: 'en',
  cn: 'cn'
};
// 清除token缓存
const clearTokenCache = () => {
  tokenCache = {
    token: null,
    expiresAt: 0
  };
  console.log('已清除 access_token 缓存');
};
const generateAccessToken = async (fastify, forceRefresh = false) => {
  // 检查缓存是否有效（提前5分钟刷新）
  const now = Date.now();
  if (!forceRefresh && tokenCache.token && tokenCache.expiresAt > now + 5 * 60 * 1000) {
    console.log('使用缓存的 access_token');
    return tokenCache.token;
  }
  // 从环境变量获取配置
  const appId = fastify.config.CHANJING_APPID;
  const secretKey = fastify.config.CHANJING_SEC;
  const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
  if (!appId || !secretKey) {
    throw new Error('蝉镜API配置不完整，请检查 CHANJING_APPID 和 CHANJING_SEC 环境变量');


智能任务调度系统 V1.0                                                    第 10 页
--------------------------------------------------------------------------------
  }
  try {
    console.log('正在获取新的 access_token...');
    // 调用蝉镜API获取 access_token
    const response = await axios.post(
      `${baseUrl}/open/v1/access_token`,
      {
        app_id: appId,
        secret_key: secretKey
      },
      {
        headers: {
          'Content-Type': 'application/json'
        }
      }
    );
    console.log('获取 access_token 响应:', response.data);
    if (response.data.code !== 0) {
      throw new Error(`获取 access_token 失败: ${response.data.msg} (错误码: ${response.data.code})`);
    }
    const { access_token, expire_in } = response.data.data;
    // 缓存 token，expires_in 是秒数，转换为毫秒
    tokenCache = {
      token: access_token,
      expiresAt: now + expire_in * 1000
    };
    console.log(`access_token 获取成功，将在 ${expire_in} 秒后过期`);
    return access_token;
  } catch (error) {
    console.error('获取 access_token 失败:', error);
    // 清除可能损坏的缓存
    clearTokenCache();
    throw new Error(`获取 access_token 失败: ${error.message}`);
  }
};
const callChanjingAPI = async (fastify, { text, digital, taskId, options = {} }, retryCount = 0) => {
  const maxRetries = 1; // 最多重试1次
  const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
  try {
    // 获取 access_token
    const accessToken = await generateAccessToken(fastify);
    console.log('Debug: 使用 access_token:', accessToken?.substring(0, 10) + '...');
    if (!accessToken) {
      throw new Error('蝉镜API access_token获取失败');
    }
    // 使用蝉镜平台的数字人ID，如果没有配置则抛出错误
    // 确保从数据库模型中正确获取字段值
    console.log('---->', digital);
    const chanjingDigitalId = digital.options.chanjingDigitalId;
    const chanjingAudioManId = digital.options.chanjingAudioManId;


智能任务调度系统 V1.0                                                    第 11 页
--------------------------------------------------------------------------------
    if (!chanjingDigitalId) {
      throw new Error(`数字人 "${digital.name}" 未配置蝉镜平台数字人ID，请在数字人管理页面配置`);
    }
    console.log('Debug: 使用蝉镜数字人ID:', chanjingDigitalId);
    console.log('Debug: 使用蝉镜音色ID:', chanjingAudioManId);
    // 生成语音文件
    // TODO: 语音支持定制，目前只有女生，先不需要
    let wavUrl = 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3';
    // if (chanjingAudioManId && options.audio?.type !== 'audio') {
    //   // 只有在有音色ID且不是使用外部音频文件时才生成语音
    //   try {
    //     const audioResult = await generateAudioAPI({
    //       text: Array.isArray(text) ? text.join('') : text,
    //       audioManId: chanjingAudioManId,
    //       speed: options.audio?.tts?.speed || 1,
    //       pitch: options.audio?.tts?.pitch || 1,
    //       audioOptions: {
    //         font_size: options.subtitle_config?.font_size || 64,
    //         width: options.subtitle_config?.width || 1000,
    //         height: options.subtitle_config?.height || 90
    //       }
    //     });
    //     wavUrl = audioResult.audioUrl;
    //     console.log('语音生成成功，将使用生成的音频文件:', wavUrl);
    //   } catch (error) {
    //     console.warn('语音生成失败，将使用TTS方式:', error.message);
    //     // 如果语音生成失败，继续使用TTS方式
    //   }
    // }
    // 构建请求数据，支持更多配置选项
    const requestData = {
      person: {
        id: chanjingDigitalId, // 使用蝉镜平台的数字人ID
        x: options.person?.x || 0,
        y: options.person?.y || 0,
        width: options.person?.width || 1080,
        height: options.person?.height || 1920, // 修正：文档默认高度为1920，不是1440
        // 支持更多数字人配置
        ...(options.person?.figure_type && { figure_type: options.person.figure_type }),
        ...(options.person?.drive_mode && { drive_mode: options.person.drive_mode }),
        ...(options.person?.is_rgba_mode !== undefined && { is_rgba_mode: options.person.is_rgba_mode }),
        ...(options.person?.backway !== undefined && { backway: options.person.backway }) // 修正：backway应该是int类型
      },
      audio: {
        tts: {
          text: Array.isArray(text) ? text : [text], // 文档说明：文本数组，所有内容放到一个字符串上面
          speed: options.audio?.tts?.speed || 1,
          audio_man: options.audio?.tts?.audio_man || chanjingAudioManId, // 使用蝉镜平台的音色ID
          ...(options.audio?.tts?.pitch && { pitch: options.audio.tts.pitch })
        }, // 支持音频文件输入


智能任务调度系统 V1.0                                                    第 12 页
--------------------------------------------------------------------------------
        // ...(options.audio?.wav_url && { wav_url: options.audio.wav_url }),
        wav_url: options.audio?.wav_url || wavUrl,
        type: options.audio?.type || 'audio',
        volume: options.audio?.volume || 100,
        language: languageMap[options.audio?.language || 'zh-CN']
        // language: options.audio?.language || 'zh-CN'
      }, // 背景配置
      bg_color: options.bg_color || '#EDEDED', // 支持背景图片
      ...(options.bg && {
        bg: {
          src_url: options.bg.src_url,
          x: options.bg.x || 0,
          y: options.bg.y || 0,
          width: options.bg.width || 1080,
          height: options.bg.height || 1920
        }
      }), // 字幕配置 - 根据文档，这是可选参数
      ...(options.subtitle_config && {
        subtitle_config: {
          x: options.subtitle_config.x !== undefined ? options.subtitle_config.x : 31,
          y: options.subtitle_config.y !== undefined ? options.subtitle_config.y : 1521,
          show: options.subtitle_config.show !== undefined ? options.subtitle_config.show : true,
          width: options.subtitle_config.width !== undefined ? options.subtitle_config.width : 1000,
          height: options.subtitle_config.height !== undefined ? options.subtitle_config.height : 200,
          font_size: options.subtitle_config.font_size !== undefined ? options.subtitle_config.font_size : 64,
          color: options.subtitle_config.color || '#000000',
          ...(options.subtitle_config.stroke_color && { stroke_color: options.subtitle_config.stroke_color }),
          ...(options.subtitle_config.stroke_width !== undefined && { stroke_width: options.subtitle_config.stroke_width }),
          ...(options.subtitle_config.font_id && { font_id: options.subtitle_config.font_id })
        }
      }),
      screen_width: options.screen_width || 1080,
      screen_height: options.screen_height || 1920,
      callback: `${fastify.config.BASE_URL}/api/v1/callback/chanjing-video/${taskId}`
    };
    console.log('蝉镜API请求数据:', JSON.stringify(requestData, null, 2));
    const response = await axios.post(`${baseUrl}/open/v1/create_video`, requestData, {
      headers: {
        'Content-Type': 'application/json',
        access_token: accessToken
      }
    });
    console.log('蝉镜API响应:', response.data);
    if (response.data.code !== 0) {
      // 根据错误码提供更详细的错误信息，补充更多错误码
      const errorMessages = {
        400: '传入参数格式错误',
        10400: 'AccessToken验证失败',
        40000: '参数错误',
        40001: '超出QPS限制',


智能任务调度系统 V1.0                                                    第 13 页
--------------------------------------------------------------------------------
        40002: '制作视频时长到达上限',
        50000: '系统内部错误',
        50011: '作品存在不合法规文字内容'
      };
      const errorMsg = errorMessages[response.data.code] || response.data.msg || '蝉镜API调用失败';
      // 如果是token验证失败且还有重试次数，清除缓存并重试
      if (response.data.code === 10400 && retryCount < maxRetries) {
        console.log('AccessToken验证失败，清除缓存并重试...');
        clearTokenCache();
        return await callChanjingAPI(fastify, { text, digital, taskId, options }, retryCount + 1);
      }
      throw new Error(`${errorMsg} (错误码: ${response.data.code})`);
    }
    return response.data;
  } catch (error) {
    // 如果是网络错误或认证错误，且还有重试次数，尝试重新获取token并重试
    if (retryCount < maxRetries && (error.response?.status === 401 || error.response?.status === 403 || error.message?.includes('AccessToken验证失败') || error.message?.includes('access_token'))) {
      console.log('检测到认证错误，清除缓存并重试...');
      clearTokenCache();
      return await callChanjingAPI(fastify, { text, digital, taskId, options }, retryCount + 1);
    }
    // 其他错误直接抛出
    throw error;
  }
};
// 获取视频状态描述
const getVideoStatusDescription = status => {
  const statusMap = {
    10: '生成中',
    30: '生成成功',
    40: '参数异常',
    41: '参数异常',
    42: '参数异常',
    43: '参数异常',
    44: '参数异常',
    45: '参数异常',
    46: '参数异常',
    47: '参数异常',
    48: '参数异常',
    49: '参数异常',
    50: '服务异常',
    51: '服务异常',
    52: '服务异常',
    53: '服务异常',
    54: '服务异常',
    55: '服务异常',
    56: '服务异常',
    57: '服务异常',
    58: '服务异常',
    59: '服务异常'


智能任务调度系统 V1.0                                                    第 14 页
--------------------------------------------------------------------------------
  };
  return statusMap[status] || `未知状态(${status})`;
};
// 通过禅境的API，获取视频生成的结果，跟callback模式可以兼容，两边不要冲突
const queryChanjingVideoTaskResult = async (fastify, videoId, retryCount = 0) => {
  const maxRetries = 1; // 最多重试1次
  try {
    // 获取 access_token
    const accessToken = await generateAccessToken(fastify);
    const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
    console.log(`查询视频详情，视频ID: ${videoId}`);
    // 调用蝉镜API查询视频详情
    const response = await axios.get(`${baseUrl}/open/v1/video`, {
      params: {
        id: videoId
      },
      headers: {
        access_token: accessToken
      }
    });
    console.log('查询视频详情响应:', response.data);
    if (response.data.code !== 0) {
      const errorMessages = {
        10400: 'AccessToken验证失败',
        40000: '参数错误',
        50000: '系统内部错误'
      };
      const errorMsg = errorMessages[response.data.code] || response.data.msg || '查询视频详情失败';
      // 如果是token验证失败且还有重试次数，清除缓存并重试
      if (response.data.code === 10400 && retryCount < maxRetries) {
        console.log('查询视频详情时AccessToken验证失败，清除缓存并重试...');
        clearTokenCache();
        return await queryChanjingVideoTaskResult(fastify, videoId, retryCount + 1);
      }
      throw new Error(`${errorMsg} (错误码: ${response.data.code})`);
    }
    const videoData = response.data.data;
    // 处理字幕数据
    let subtitles = [];
    if (videoData.subtitle_data_url) {
      try {
        // 下载字幕数据
        const subtitleResponse = await axios.get(videoData.subtitle_data_url);
        subtitles = subtitleResponse.data || [];
        console.log(`成功获取字幕数据，共 ${subtitles.length} 条`);
      } catch (error) {
        console.warn('获取字幕数据失败:', error.message);
      }
    }
    // 返回格式化的视频详情


智能任务调度系统 V1.0                                                    第 15 页
--------------------------------------------------------------------------------
    return {
      id: videoData.id,
      status: videoData.status,
      progress: videoData.progress,
      msg: videoData.msg || '',
      videoUrl: videoData.video_url,
      previewUrl: videoData.preview_url,
      duration: videoData.duration,
      createTime: videoData.create_time,
      subtitleDataUrl: videoData.subtitle_data_url,
      subtitles: subtitles, // 状态描述
      statusDescription: getVideoStatusDescription(videoData.status), // 是否完成
      isCompleted: videoData.status === 30, // 是否失败
      isFailed: videoData.status >= 40 && videoData.status < 60, // 是否生成中
      isProcessing: videoData.status === 10
    };
  } catch (error) {
    // 如果是网络错误或认证错误，且还有重试次数，尝试重新获取token并重试
    if (retryCount < maxRetries && (error.response?.status === 401 || error.response?.status === 403 || error.message?.includes('AccessToken验证失败') || error.message?.includes('access_token'))) {
      console.log('查询视频详情时检测到认证错误，清除缓存并重试...');
      clearTokenCache();
      return await queryChanjingVideoTaskResult(fastify, videoId, retryCount + 1);
    }
    console.error('查询视频详情失败:', error);
    throw error;
  }
};
const runner = async (fastify, optionsRaw, { task, polling, progress }) => {
  // const host = 'http://18.143.57.170:34570';
  const { question, digital } = task.input;
  const bgUrl = digital.background ? await fastify.fileManager.services.getFileUrl({ id: digital.background }) : null;
  const videoOptions = Object.assign({
    person: {
      figure_type: 'sit_body', // 临时写死了，当前用到数字人形象都是有这个type
      x: 0,
      y: 0,
      width: 1080,
      height: 1920
    },
    audio: {
      tts: {
        speed: 1
      },
      type: 'tts',
      wav_url: 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3', // 临时音色
      volume: 100,
      language: languageMap[digital.language || 'cn']
    },
    bg_color: '#EDEDED',
    bg: bgUrl


智能任务调度系统 V1.0                                                    第 16 页
--------------------------------------------------------------------------------
      ? {
          x: 0,
          y: 0,
          height: 1920,
          width: 1080,
          src_url: bgUrl
        }
      : null,
    subtitle_config: {
      x: 31,
      y: 1521,
      show: false,
      width: 1000,
      height: 200,
      font_size: 64,
      color: '#000000'
    },
    screen_width: 1080,
    screen_height: 1920
  });
  // 构建蝉镜API选项，支持更多配置
  const chanjingOptions = {
    // 数字人配置
    person: {
      x: videoOptions.person?.x,
      y: videoOptions.person?.y,
      width: videoOptions.person?.width,
      height: videoOptions.person?.height,
      figure_type: videoOptions.person?.figure_type || 'whole_body',
      drive_mode: videoOptions.person?.drive_mode,
      is_rgba_mode: videoOptions.person?.is_rgba_mode,
      backway: videoOptions.person?.backway
    }, // 音频配置
    audio: {
      tts: {
        speed: videoOptions.audio?.tts?.speed,
        audio_man: videoOptions.audio?.tts?.audio_man, // 使用数字人的音色ID
        pitch: videoOptions.audio?.tts?.pitch
      },
      wav_url: videoOptions.audio?.wav_url,
      type: videoOptions.audio?.type,
      volume: videoOptions.audio?.volume,
      language: languageMap[videoOptions.audio?.language || digital.language || 'cn']
    }, // 背景配置
    bg_color: videoOptions.bg_color,
    bg: videoOptions.bg, // 字幕配置
    subtitle_config: videoOptions.subtitle_config, // 屏幕尺寸
    screen_width: videoOptions.screen_width,
    screen_height: videoOptions.screen_height
  };


智能任务调度系统 V1.0                                                    第 17 页
--------------------------------------------------------------------------------
  // 调用禅镜API
  const videoGenerationResult = await callChanjingAPI(fastify, {
    text: question.title,
    digital: digital,
    taskId: task.id,
    options: chanjingOptions
  });
  const chanjingTaskId = videoGenerationResult.data;
  //   const chanjingTraceId = videoGenerationResult.trace_id;
  return await polling(async () => {
    try {
      const videoResult = await queryChanjingVideoTaskResult(fastify, chanjingTaskId);
      console.log('蝉镜视频任务查询结果:', videoResult);
      // 根据视频状态更新任务和关联记录
      if (videoResult.isCompleted) {
        // 视频生成成功
        if (!videoResult.videoUrl) {
          throw new Error('视频生成完成但未获取到视频URL');
        }
        console.log(`任务处理成功，视频URL: ${videoResult.videoUrl}`);
        const file = await fastify.fileManager.services.uploadFromUrl({ url: videoResult.videoUrl });
        return { result: 'success', data: Object.assign({}, videoResult, { video: file.id }) };
      } else if (videoResult.isFailed) {
        // 视频生成失败
        const errorMsg = videoResult.msg || `视频生成失败，状态: ${videoResult.statusDescription}`;
        return { result: 'failed', message: errorMsg };
      } else if (videoResult.isProcessing) {
        // TODO: 视频还在生成中
        const progressMsg = `视频生成中，进度: ${videoResult.progress}%`;
        await progress(videoResult.progress);
        console.log('task info:', progressMsg);
        return;
      } else {
        // 其他未知状态
        console.log('task unknown status');
        return;
      }
    } catch (error) {
      console.error('task error: ', error);
      return;
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/digital/interface1.js
const runner = async (fastify, options, { task, polling, updateProgress }) => {
  const { question, digital } = task.input;
  const videoUrl = await fastify.fileManager.services.getFileUrl({ id: digital.waitingVideo });
  const backgroundUrl = await fastify.fileManager.services.getFileUrl({ id: digital.background });


智能任务调度系统 V1.0                                                    第 18 页
--------------------------------------------------------------------------------
  const response = await fetch(`${fastify.config.TASK_DIGITAL_HOST}/api/cj/avatar_video`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      text: question.title,
      language: (digital.language || '').toLowerCase().indexOf('cn') > -1 ? 'cn' : 'en',
      gender: digital.options.gender || 'female',
      width: digital.options.sizeType === 'horizontal' ? 1600 : 900,
      height: digital.options.sizeType === 'horizontal' ? 900 : 1600,
      original_video_url: videoUrl,
      background_url: backgroundUrl
    })
  });
  if (!response.ok) {
    throw new Error(`${String(response.status)}:${await response.text()}`);
  }
  const responseData = await response.json();
  if (responseData.status_code !== 200) {
    throw new Error(responseData.message || responseData.msg);
  }
  const videoId = responseData.data.video_id;
  return await polling(async () => {
    const response = await fetch(`${fastify.config.TASK_DIGITAL_HOST}/api/cj/avatar_video_detail/${videoId}`, {
      method: 'GET'
    });
    if (!response.ok) {
      return;
    }
    const responseData = await response.json();
    if (responseData.status_code !== 200 || String(responseData.data.status) === '40') {
      return { result: 'failed', message: responseData.data?.msg || responseData.message || responseData.msg };
    }
    if (String(responseData.data.status) === '30') {
      const file = await fastify.fileManager.services.uploadFromUrl({
        url: responseData.data.video_url
      });
      return { result: 'success', data: Object.assign({}, responseData.data, { video: file.id }) };
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/probe/interface2.js
const qs = require('qs');
const runner = async (fastify, options, { task, polling }) => {
  const { userId, title, language, probeQuestionsNumber, existingProbeQuestions } = task.input;
  const url = 'https://api.coze.cn/v3/chat';
  const headers = {


智能任务调度系统 V1.0                                                    第 19 页
--------------------------------------------------------------------------------
    Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
    'Content-Type': 'application/json'
  };
  const body = {
    bot_id: '7538380545018232858',
    user_id: userId,
    stream: false,
    additional_messages: [
      {
        content: JSON.stringify({
          INTERVIEW_QUESTION: title,
          LANGUAGE: language,
          PROBE_QUESTIONS_NUMBER: probeQuestionsNumber,
          EXISTING_PROBE_QUESTIONS: existingProbeQuestions
        }),
        content_type: 'text',
        role: 'user',
        type: 'question'
      }
    ],
    parameters: {}
  };
  const response = await fetch(url, {
    method: 'POST',
    headers: headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  if (!response.ok) {
    await task.update({
      status: 'failed',
      error: response.status
    });
    return;
  }
  const resData = await response.json();
  if (resData.code !== 0) {
    await task.update({
      status: 'failed',
      error: resData.msg
    });
    return;
  }
  return await polling(async () => {
    const pollResponse = await fetch(
      `${url}/retrieve?${qs.stringify({
        conversation_id: resData.data?.conversation_id,
        chat_id: resData.data?.id


智能任务调度系统 V1.0                                                    第 20 页
--------------------------------------------------------------------------------
      })}`,
      {
        method: 'GET',
        headers: headers
      }
    );
    if (!pollResponse.ok) {
      return;
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      const resultResponse = await fetch(
        `${url}/message/list?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',
          headers: headers
        }
      );
      if (!resultResponse.ok) {
        return { result: 'failed', message: resultResponse.status };
      }
      const resultResData = await resultResponse.json();
      console.log('resultResData--------------', resultResData);
      if (resultResData.code !== 0) {
        return { result: 'failed', message: resultResData };
      }
      const probeQuestions = (JSON.parse(resultResData.data[0].content)?.ProbesQuestions || []).map(({ QuestionContent, ...question }) => ({ ...question, title: QuestionContent }));
      return { result: 'success', data: probeQuestions };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', message: pollData.msg || '任务处理失败' };
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/probe/interface1.js
const runner = async (fastify, options, { task, polling }) => {
  const host = fastify.config.TASK_FLOW_UP_HOST;
  const { title, language } = task.input;
  const response = await fetch(`${host}/api/v1/generate/create`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      question: title,


智能任务调度系统 V1.0                                                    第 21 页
--------------------------------------------------------------------------------
      language
    })
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const data = await response.json();
  if (data.code !== 0) {
    throw new Error(data);
  }
  const taskId = data.data.tid;
  return await polling(async () => {
    const pollResponse = await fetch(`${host}/api/v1/generate/result?tid=${taskId}`, {
      method: 'GET'
    });
    if (!pollResponse.ok) {
      return {};
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      return { result: 'success', data: pollData.data.followup };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', data: pollData.message };
    }
  });
};
module.exports = runner;

// 文件: server/libs/services/task.js
const fp = require('fastify-plugin');
const get = require('lodash/get');
const fs = require('fs-extra');
const path = require('node:path');
const languageMap = {
  'en-US': 'en',
  'zh-CN': 'cn',
  en: 'en',
  cn: 'cn'
};
module.exports = fp(async (fastify, options) => {
  const { models, services } = fastify[options.name];
  const { Op } = fastify.sequelize.Sequelize;
  // 处理系统任务的函数
  const processSystemTask = async task => {
    if (task.status !== 'pending') {
      return;
    }
    const taskModulePath = path.resolve(process.cwd(), 'libs', 'tasks', task.type, `${options.remoteTask}.js`);
    if (!(await fs.exists(taskModulePath))) {


智能任务调度系统 V1.0                                                    第 22 页
--------------------------------------------------------------------------------
      await task.update({
        status: 'failed',
        error: '未匹配到任务执行器'
      });
    }
    await task.update({
      status: 'running',
      progress: 0
    });
    require(taskModulePath)(fastify, options, {
      task,
      callback: result => {}
    }).catch(error => {
      return task.update({
        status: 'failed',
        error: error.toString()
      });
    });
    try {
      // 目前只处理数字人视频生成任务
      if (task.type === 'digital' && task.status === 'pending') {
        const { question, digital: originalDigital } = task.input;
        const digital = Object.assign({}, originalDigital, {
          language: languageMap[originalDigital.language || 'zh-CN']
        });
        // 更新任务状态为运行中
        await task.update({
          status: 'running',
          progress: 5,
          msg: '正在准备生成视频...'
        });
        fastify.log.info(`开始自动处理系统任务 ID: ${task.id}, 类型: ${task.type}, 问题: "${question.title}", 数字人: "${digital.name}"`);
        // 调用禅镜API生成视频
        try {
          const bgUrl = digital.background ? await fastify.fileManager.services.getFileUrl({ id: digital.background }) : null;
          // 构建蝉镜API选项，根据最新文档修正参数
          const chanjingOptions = {
            person: {
              figure_type: 'sit_body', // 临时写死了，当前用到数字人形象都是有这个type
              x: 0,
              y: 0,
              width: 1080,
              height: 1920
            },
            audio: {
              tts: {
                speed: 1
              },
              type: 'tts',
              wav_url: 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3', // 临时音色


智能任务调度系统 V1.0                                                    第 23 页
--------------------------------------------------------------------------------
              volume: 100,
              language: digital.language
            },
            bg_color: '#EDEDED',
            bg: bgUrl
              ? {
                  x: 0,
                  y: 0,
                  height: 1920,
                  width: 1080,
                  src_url: bgUrl
                }
              : null,
            subtitle_config: {
              x: 31,
              y: 1521,
              show: false,
              width: 1000,
              height: 200,
              font_size: 64,
              color: '#000000'
            },
            screen_width: 1080,
            screen_height: 1920
          };
          // 调用禅镜API
          const videoGenerationResult = await services.question.callChanjingAPI({
            text: question.title,
            digital: digital,
            taskId: task.id,
            options: chanjingOptions
          });
          // 更新任务状态
          await task.update({
            progress: 10,
            msg: '视频生成任务已提交，等待处理...',
            options: {
              ...task.options,
              chanjingTaskId: videoGenerationResult.data,
              chanjingTraceId: videoGenerationResult.trace_id
            }
          });
          fastify.log.info(`系统任务 ${task.id} 已提交到禅镜API，任务ID: ${videoGenerationResult.data}`);
          return true;
        } catch (error) {
          // 更新任务状态为失败
          await task.update({
            status: 'failed',
            progress: 0,
            msg: `视频生成失败: ${error.message}`


智能任务调度系统 V1.0                                                    第 24 页
--------------------------------------------------------------------------------
          });
          fastify.log.error(`系统任务 ${task.id} 处理失败:`, error);
          return false;
        }
      }
      // 处理报告生成任务
      if (task.type === 'report' && task.status === 'pending') {
        // 这里调用报告生成API
        await task.update({
          status: 'running',
          progress: 0
        });
        //这里不阻塞后面的任务执行
        require(`../plugins/report/${options.remoteTask}`)(fastify, options, {
          task,
          callback: async report => {
            const { clientUserId } = task.input;
            const clientUser = await models.clientUser.findByPk(clientUserId);
            clientUser.report = report;
            clientUser.status = 'ended';
            await clientUser.save();
          }
        });
      }
    } catch (error) {
      await task.update({
        status: 'failed',
        progress: 0,
        msg: `任务处理失败: ${error.toString()}`
      });
    }
  };
  // runnerType: system or manual
  // generateProbe: 是否同时生成追问问题数字人
  const createDigital = async (authenticatePayload, { digitalId, questionIds, runnerType, generateProbe }) => {
    const { id: userId } = authenticatePayload;
    const digital = await services.digital.detail({ id: digitalId });
    // 如果需要生成追问，查询所有主问题的追问问题ID
    let allProbeQuestionIds = [];
    if (generateProbe) {
      const probeQuestions = await models.question.findAll({
        where: {
          parentId: {
            [Op.in]: questionIds
          },
          type: 'probe'
        },
        attributes: ['id']
      });
      allProbeQuestionIds = probeQuestions.map(q => q.id);


智能任务调度系统 V1.0                                                    第 25 页
--------------------------------------------------------------------------------
    }
    // 查询主问题和追问问题的当前任务
    const targetIds = [...questionIds, ...allProbeQuestionIds];
    const currentTasks = await models.task.findAll({
      where: {
        type: 'digital',
        targetId: {
          [Op.in]: targetIds
        },
        status: {
          [Op.in]: ['pending', 'running']
        }
      }
    });
    const result = { cancel: 0, create: 0 };
    for (const questionId of questionIds) {
      const question = await services.question.detail(authenticatePayload, { id: questionId });
      if (!question) {
        continue;
      }
      const needCancel = currentTasks.filter(item => {
        return item.targetId === questionId && get(item, 'input.digital.id') === digitalId;
      });
      if (needCancel.length > 0) {
        for (const item of needCancel) {
          await item.update({ status: 'canceled' });
          result.cancel += 1;
        }
      }
      await fastify.task.services.create({
        type: 'digital',
        targetId: questionId,
        runnerType: runnerType || digital.supply,
        input: {
          name: `${question.title}->${digital.name}`,
          question,
          digital
        },
        userId,
        status: 'pending',
        progress: 0
      });
      result.create += 1;
      // 创建新任务
      // const task = await models.task.create({
      //   type: 'digital',
      //   targetId: questionId,
      //   runnerType: runnerType || digital.supply,
      //   input: {
      //     name: `${question.title}->${digital.name}`,


智能任务调度系统 V1.0                                                    第 26 页
--------------------------------------------------------------------------------
      //     question,
      //     digital
      //   },
      //   userId,
      //   status: 'pending',
      //   progress: 0
      // });
      // result.create += 1;
      // // 如果是系统任务，自动处理
      // if (task.runnerType === 'system') {
      //   // 异步处理系统任务，不阻塞当前请求
      //   processSystemTask(task).catch(err => {
      //     fastify.log.error(`自动处理系统任务 ${task.id} 失败:`, err);
      //   });
      // }
    }
    return result;
  };
  const completeDigital = async (authenticatePayload, { id, video }) => {
    const { id: userId } = authenticatePayload;
    const task = await detail(authenticatePayload, { id });
    const { digital, question } = task.input;
    let target = await models.questionDigital.findOne({
      where: {
        questionId: question.id,
        digitalId: digital.id
      }
    });
    if (!target) {
      target = await models.questionDigital.create({
        video,
        questionId: question.id,
        digitalId: digital.id,
        text: question.title
      });
    } else {
      await target.update({ video, text: question.title });
    }
    await task.update({
      status: 'success',
      completedAt: new Date(),
      output: { video, text: question.title },
      completedUserId: userId
    });
    return target;
  };
  // 手动触发，检查chanjingTask视频生成的结果【因为本地调试无法使用callback模式】
  // 手动触发，检查chanjingTask视频生成的结果【因为本地调试无法使用callback模式】
  const checkChanjingDigitalTaskResult = async (authenticatePayload, { id, chanjingTaskId }) => {
    try {


智能任务调度系统 V1.0                                                    第 27 页
--------------------------------------------------------------------------------
      const { id: userId } = authenticatePayload;
      // 获取任务详情
      const task = await detail(authenticatePayload, { id });
      if (!task) {
        throw new Error('任务不存在或者尚未生成');
      }
      const { digital, question } = task.input;
      // 查找问题数字人关联记录
      let target = await models.questionDigital.findOne({
        where: {
          questionId: question.id,
          digitalId: digital.id
        }
      });
      if (!target) {
        target = await models.questionDigital.create({
          questionId: question.id,
          digitalId: digital.id,
          text: question.title
        });
      }
      // 调用服务查询蝉镜任务结果
      console.log(`开始查询蝉镜视频任务结果，任务ID: ${chanjingTaskId}`);
      const videoResult = await services.question.queryChanjingVideoTaskResult(chanjingTaskId);
      console.log('蝉镜视频任务查询结果:', videoResult);
      // 根据视频状态更新任务和关联记录
      if (videoResult.isCompleted) {
        // 视频生成成功
        if (!videoResult.videoUrl) {
          throw new Error('视频生成完成但未获取到视频URL');
        }
        // 更新问题数字人关联记录
        await target.update({
          video: videoResult.videoUrl,
          text: question.title,
          options: {
            ...target.options,
            chanjingResult: {
              video_url: videoResult.videoUrl,
              preview_url: videoResult.previewUrl,
              duration: videoResult.duration,
              subtitle_data_url: videoResult.subtitleDataUrl,
              subtitles: videoResult.subtitles,
              create_time: videoResult.createTime,
              chanjing_task_id: chanjingTaskId,
              status: videoResult.status,
              progress: videoResult.progress
            }
          }
        });


智能任务调度系统 V1.0                                                    第 28 页
--------------------------------------------------------------------------------
        // 更新任务状态为成功
        await task.update({
          status: 'success',
          progress: 100,
          completedAt: new Date(),
          output: {
            video: videoResult.videoUrl,
            text: question.title,
            duration: videoResult.duration,
            previewUrl: videoResult.previewUrl
          },
          completedUserId: userId,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理成功，视频URL: ${videoResult.videoUrl}`);
        return {
          success: true,
          message: '视频生成成功',
          task: task,
          target: target,
          videoResult: videoResult
        };
      } else if (videoResult.isFailed) {
        // 视频生成失败
        const errorMsg = videoResult.msg || `视频生成失败，状态: ${videoResult.statusDescription}`;
        // 更新任务状态为失败
        await task.update({
          status: 'failed',
          progress: 0,
          msg: errorMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理失败: ${errorMsg}`);
        return {
          success: false,
          message: errorMsg,
          task: task,
          target: target,
          videoResult: videoResult
        };
      } else if (videoResult.isProcessing) {
        // 视频还在生成中
        const progressMsg = `视频生成中，进度: ${videoResult.progress}%`;
        // 更新任务进度


智能任务调度系统 V1.0                                                    第 29 页
--------------------------------------------------------------------------------
        await task.update({
          status: 'running',
          progress: videoResult.progress,
          msg: progressMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理中: ${progressMsg}`);
        return {
          success: false,
          message: progressMsg,
          task: task,
          target: target,
          videoResult: videoResult,
          isProcessing: true
        };
      } else {
        // 其他未知状态
        const statusMsg = `视频状态未知: ${videoResult.statusDescription}`;
        // 更新任务状态
        await task.update({
          status: 'running',
          progress: videoResult.progress || 0,
          msg: statusMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 状态未知: ${statusMsg}`);
        return {
          success: false,
          message: statusMsg,
          task: task,
          target: target,
          videoResult: videoResult
        };
      }
    } catch (error) {
      console.error('检查蝉镜数字人任务结果失败:', error);
      // 尝试更新任务状态为失败（如果任务存在的话）
      try {
        const task = await models.task.findByPk(id);
        if (task) {
          await task.update({
            status: 'failed',
            progress: 0,
            msg: `检查任务结果失败: ${error.message}`


智能任务调度系统 V1.0                                                    第 30 页
--------------------------------------------------------------------------------
          });
        }
      } catch (updateError) {
        console.error('更新任务状态失败:', updateError);
      }
      throw error;
    }
  };
  const detail = async (authenticatePayload, { id }) => {
    const task = await models.task.findByPk(id);
    if (!task) {
      throw new Error('任务不存在');
    }
    return task;
  };
  const cancel = async (authenticatePayload, { id }) => {
    const task = await detail(authenticatePayload, { id });
    return await task.update({
      status: 'canceled'
    });
  };
  const list = async (authenticatePayload, { filter, perPage, currentPage }) => {
    const whereQuery = {};
    ['targetId', 'type', 'status', 'runnerType'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = filter[key];
      }
    });
    const { rows, count } = await models.task.findAndCountAll({
      where: Object.assign({}, whereQuery),
      offset: perPage * (currentPage - 1),
      limit: perPage,
      order: [['createdAt', 'DESC']]
    });
    return {
      pageData: rows,
      totalCount: count
    };
  };
  // 处理所有待处理的系统任务
  const processPendingSystemTasks = async () => {
    try {
      // 查找所有待处理的系统任务
      const pendingTasks = await models.task.findAll({
        where: {
          runnerType: 'system',
          status: 'pending'
        },
        limit: 10 // 每次处理10个任务，避免过载
      });



================================================================================

## 后30页


智能任务调度系统 V1.0                                                    第 31 页
--------------------------------------------------------------------------------
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const data = await response.json();
  if (data.code !== 0) {
    throw new Error(data);
  }
  return data.data;
};
module.exports = runner;

// 文件: server/libs/tasks/report/interface2.js
const transform = require('lodash/transform');
const qs = require('qs');
const runner = async (fastify, options, { task, polling }) => {
  const { clientUserId, questionList, description } = task.input;
  const url = 'https://api.coze.cn/v3/chat';
  const { questions, answers } = transform(
    questionList,
    (result, value) => {
      const { answer, title } = value;
      const questions = [],
        answers = [];
      if (result['questions']) {
        questions.push(...result['questions']);
      }
      if (result['answers']) {
        answers.push(...result['answers']);
      }
      questions.push(title);
      answers.push((answer || []).map(({ message }) => message).join(''));
      result['questions'] = questions;
      result['answers'] = answers;
    },
    {}
  );
  const headers = {
    Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
    'Content-Type': 'application/json'
  };
  const body = {
    bot_id: '7532048924065611812',
    user_id: clientUserId,
    stream: false,
    additional_messages: [
      {
        content: JSON.stringify({
          INTERVIEW_SCENE: description,
          INTERVIEW_QUESTIONS: questions,
          USER_ANSWERS: answers
        }),


智能任务调度系统 V1.0                                                    第 32 页
--------------------------------------------------------------------------------
        content_type: 'text',
        role: 'user',
        type: 'question'
      }
    ],
    parameters: {}
  };
  const response = await fetch(url, {
    method: 'POST',
    headers: headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    await task.update({
      status: 'failed',
      error: response.status
    });
    return;
  }
  const resData = await response.json();
  if (resData.code !== 0) {
    await task.update({
      status: 'failed',
      error: resData.msg
    });
    return;
  }
  return await polling(async () => {
    const pollResponse = await fetch(
      `https://api.coze.cn/v3/chat/retrieve?${qs.stringify({
        conversation_id: resData.data?.conversation_id,
        chat_id: resData.data?.id
      })}`,
      {
        method: 'GET',
        headers: headers
      }
    );
    if (!pollResponse.ok) {
      return;
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      const resultResponse = await fetch(
        `https://api.coze.cn/v3/chat/message/list?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',


智能任务调度系统 V1.0                                                    第 33 页
--------------------------------------------------------------------------------
          headers: headers
        }
      );
      if (!resultResponse.ok) {
        return { result: 'failed', message: resultResponse.status };
      }
      const resultResData = await resultResponse.json();
      if (resultResData.code !== 0) {
        return { result: 'failed', message: resultResData };
      }
      return { result: 'success', data: JSON.parse(resultResData.data[0].content) };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', message: pollData.msg || '任务处理失败' };
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/report/interface1.js
const runner = async (fastify, options, { task }) => {
  throw new Error('暂不支持报告');
};
module.exports = runner;

// 文件: server/libs/tasks/digital/interface2.js
const axios = require('axios');
// 内存缓存 access_token
let tokenCache = {
  token: null,
  expiresAt: 0
};
const languageMap = {
  'en-US': 'en',
  'zh-CN': 'cn',
  en: 'en',
  cn: 'cn'
};
// 清除token缓存
const clearTokenCache = () => {
  tokenCache = {
    token: null,
    expiresAt: 0
  };
  console.log('已清除 access_token 缓存');
};
const generateAccessToken = async (fastify, forceRefresh = false) => {
  // 检查缓存是否有效（提前5分钟刷新）
  const now = Date.now();
  if (!forceRefresh && tokenCache.token && tokenCache.expiresAt > now + 5 * 60 * 1000) {


智能任务调度系统 V1.0                                                    第 34 页
--------------------------------------------------------------------------------
    console.log('使用缓存的 access_token');
    return tokenCache.token;
  }
  // 从环境变量获取配置
  const appId = fastify.config.CHANJING_APPID;
  const secretKey = fastify.config.CHANJING_SEC;
  const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
  if (!appId || !secretKey) {
    throw new Error('蝉镜API配置不完整，请检查 CHANJING_APPID 和 CHANJING_SEC 环境变量');
  }
  try {
    console.log('正在获取新的 access_token...');
    // 调用蝉镜API获取 access_token
    const response = await axios.post(
      `${baseUrl}/open/v1/access_token`,
      {
        app_id: appId,
        secret_key: secretKey
      },
      {
        headers: {
          'Content-Type': 'application/json'
        }
      }
    );
    console.log('获取 access_token 响应:', response.data);
    if (response.data.code !== 0) {
      throw new Error(`获取 access_token 失败: ${response.data.msg} (错误码: ${response.data.code})`);
    }
    const { access_token, expire_in } = response.data.data;
    // 缓存 token，expires_in 是秒数，转换为毫秒
    tokenCache = {
      token: access_token,
      expiresAt: now + expire_in * 1000
    };
    console.log(`access_token 获取成功，将在 ${expire_in} 秒后过期`);
    return access_token;
  } catch (error) {
    console.error('获取 access_token 失败:', error);
    // 清除可能损坏的缓存
    clearTokenCache();
    throw new Error(`获取 access_token 失败: ${error.message}`);
  }
};
const callChanjingAPI = async (fastify, { text, digital, taskId, options = {} }, retryCount = 0) => {
  const maxRetries = 1; // 最多重试1次
  const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
  try {
    // 获取 access_token
    const accessToken = await generateAccessToken(fastify);


智能任务调度系统 V1.0                                                    第 35 页
--------------------------------------------------------------------------------
    console.log('Debug: 使用 access_token:', accessToken?.substring(0, 10) + '...');
    if (!accessToken) {
      throw new Error('蝉镜API access_token获取失败');
    }
    // 使用蝉镜平台的数字人ID，如果没有配置则抛出错误
    // 确保从数据库模型中正确获取字段值
    console.log('---->', digital);
    const chanjingDigitalId = digital.options.chanjingDigitalId;
    const chanjingAudioManId = digital.options.chanjingAudioManId;
    if (!chanjingDigitalId) {
      throw new Error(`数字人 "${digital.name}" 未配置蝉镜平台数字人ID，请在数字人管理页面配置`);
    }
    console.log('Debug: 使用蝉镜数字人ID:', chanjingDigitalId);
    console.log('Debug: 使用蝉镜音色ID:', chanjingAudioManId);
    // 生成语音文件
    // TODO: 语音支持定制，目前只有女生，先不需要
    let wavUrl = 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3';
    // if (chanjingAudioManId && options.audio?.type !== 'audio') {
    //   // 只有在有音色ID且不是使用外部音频文件时才生成语音
    //   try {
    //     const audioResult = await generateAudioAPI({
    //       text: Array.isArray(text) ? text.join('') : text,
    //       audioManId: chanjingAudioManId,
    //       speed: options.audio?.tts?.speed || 1,
    //       pitch: options.audio?.tts?.pitch || 1,
    //       audioOptions: {
    //         font_size: options.subtitle_config?.font_size || 64,
    //         width: options.subtitle_config?.width || 1000,
    //         height: options.subtitle_config?.height || 90
    //       }
    //     });
    //     wavUrl = audioResult.audioUrl;
    //     console.log('语音生成成功，将使用生成的音频文件:', wavUrl);
    //   } catch (error) {
    //     console.warn('语音生成失败，将使用TTS方式:', error.message);
    //     // 如果语音生成失败，继续使用TTS方式
    //   }
    // }
    // 构建请求数据，支持更多配置选项
    const requestData = {
      person: {
        id: chanjingDigitalId, // 使用蝉镜平台的数字人ID
        x: options.person?.x || 0,
        y: options.person?.y || 0,
        width: options.person?.width || 1080,
        height: options.person?.height || 1920, // 修正：文档默认高度为1920，不是1440
        // 支持更多数字人配置
        ...(options.person?.figure_type && { figure_type: options.person.figure_type }),
        ...(options.person?.drive_mode && { drive_mode: options.person.drive_mode }),
        ...(options.person?.is_rgba_mode !== undefined && { is_rgba_mode: options.person.is_rgba_mode }),


智能任务调度系统 V1.0                                                    第 36 页
--------------------------------------------------------------------------------
        ...(options.person?.backway !== undefined && { backway: options.person.backway }) // 修正：backway应该是int类型
      },
      audio: {
        tts: {
          text: Array.isArray(text) ? text : [text], // 文档说明：文本数组，所有内容放到一个字符串上面
          speed: options.audio?.tts?.speed || 1,
          audio_man: options.audio?.tts?.audio_man || chanjingAudioManId, // 使用蝉镜平台的音色ID
          ...(options.audio?.tts?.pitch && { pitch: options.audio.tts.pitch })
        }, // 支持音频文件输入
        // ...(options.audio?.wav_url && { wav_url: options.audio.wav_url }),
        wav_url: options.audio?.wav_url || wavUrl,
        type: options.audio?.type || 'audio',
        volume: options.audio?.volume || 100,
        language: languageMap[options.audio?.language || 'zh-CN']
        // language: options.audio?.language || 'zh-CN'
      }, // 背景配置
      bg_color: options.bg_color || '#EDEDED', // 支持背景图片
      ...(options.bg && {
        bg: {
          src_url: options.bg.src_url,
          x: options.bg.x || 0,
          y: options.bg.y || 0,
          width: options.bg.width || 1080,
          height: options.bg.height || 1920
        }
      }), // 字幕配置 - 根据文档，这是可选参数
      ...(options.subtitle_config && {
        subtitle_config: {
          x: options.subtitle_config.x !== undefined ? options.subtitle_config.x : 31,
          y: options.subtitle_config.y !== undefined ? options.subtitle_config.y : 1521,
          show: options.subtitle_config.show !== undefined ? options.subtitle_config.show : true,
          width: options.subtitle_config.width !== undefined ? options.subtitle_config.width : 1000,
          height: options.subtitle_config.height !== undefined ? options.subtitle_config.height : 200,
          font_size: options.subtitle_config.font_size !== undefined ? options.subtitle_config.font_size : 64,
          color: options.subtitle_config.color || '#000000',
          ...(options.subtitle_config.stroke_color && { stroke_color: options.subtitle_config.stroke_color }),
          ...(options.subtitle_config.stroke_width !== undefined && { stroke_width: options.subtitle_config.stroke_width }),
          ...(options.subtitle_config.font_id && { font_id: options.subtitle_config.font_id })
        }
      }),
      screen_width: options.screen_width || 1080,
      screen_height: options.screen_height || 1920,
      callback: `${fastify.config.BASE_URL}/api/v1/callback/chanjing-video/${taskId}`
    };
    console.log('蝉镜API请求数据:', JSON.stringify(requestData, null, 2));
    const response = await axios.post(`${baseUrl}/open/v1/create_video`, requestData, {
      headers: {
        'Content-Type': 'application/json',
        access_token: accessToken
      }


智能任务调度系统 V1.0                                                    第 37 页
--------------------------------------------------------------------------------
    });
    console.log('蝉镜API响应:', response.data);
    if (response.data.code !== 0) {
      // 根据错误码提供更详细的错误信息，补充更多错误码
      const errorMessages = {
        400: '传入参数格式错误',
        10400: 'AccessToken验证失败',
        40000: '参数错误',
        40001: '超出QPS限制',
        40002: '制作视频时长到达上限',
        50000: '系统内部错误',
        50011: '作品存在不合法规文字内容'
      };
      const errorMsg = errorMessages[response.data.code] || response.data.msg || '蝉镜API调用失败';
      // 如果是token验证失败且还有重试次数，清除缓存并重试
      if (response.data.code === 10400 && retryCount < maxRetries) {
        console.log('AccessToken验证失败，清除缓存并重试...');
        clearTokenCache();
        return await callChanjingAPI(fastify, { text, digital, taskId, options }, retryCount + 1);
      }
      throw new Error(`${errorMsg} (错误码: ${response.data.code})`);
    }
    return response.data;
  } catch (error) {
    // 如果是网络错误或认证错误，且还有重试次数，尝试重新获取token并重试
    if (retryCount < maxRetries && (error.response?.status === 401 || error.response?.status === 403 || error.message?.includes('AccessToken验证失败') || error.message?.includes('access_token'))) {
      console.log('检测到认证错误，清除缓存并重试...');
      clearTokenCache();
      return await callChanjingAPI(fastify, { text, digital, taskId, options }, retryCount + 1);
    }
    // 其他错误直接抛出
    throw error;
  }
};
// 获取视频状态描述
const getVideoStatusDescription = status => {
  const statusMap = {
    10: '生成中',
    30: '生成成功',
    40: '参数异常',
    41: '参数异常',
    42: '参数异常',
    43: '参数异常',
    44: '参数异常',
    45: '参数异常',
    46: '参数异常',
    47: '参数异常',
    48: '参数异常',
    49: '参数异常',
    50: '服务异常',


智能任务调度系统 V1.0                                                    第 38 页
--------------------------------------------------------------------------------
    51: '服务异常',
    52: '服务异常',
    53: '服务异常',
    54: '服务异常',
    55: '服务异常',
    56: '服务异常',
    57: '服务异常',
    58: '服务异常',
    59: '服务异常'
  };
  return statusMap[status] || `未知状态(${status})`;
};
// 通过禅境的API，获取视频生成的结果，跟callback模式可以兼容，两边不要冲突
const queryChanjingVideoTaskResult = async (fastify, videoId, retryCount = 0) => {
  const maxRetries = 1; // 最多重试1次
  try {
    // 获取 access_token
    const accessToken = await generateAccessToken(fastify);
    const baseUrl = fastify.config.CHANJING_BASEURL || 'https://open-api.chanjing.cc';
    console.log(`查询视频详情，视频ID: ${videoId}`);
    // 调用蝉镜API查询视频详情
    const response = await axios.get(`${baseUrl}/open/v1/video`, {
      params: {
        id: videoId
      },
      headers: {
        access_token: accessToken
      }
    });
    console.log('查询视频详情响应:', response.data);
    if (response.data.code !== 0) {
      const errorMessages = {
        10400: 'AccessToken验证失败',
        40000: '参数错误',
        50000: '系统内部错误'
      };
      const errorMsg = errorMessages[response.data.code] || response.data.msg || '查询视频详情失败';
      // 如果是token验证失败且还有重试次数，清除缓存并重试
      if (response.data.code === 10400 && retryCount < maxRetries) {
        console.log('查询视频详情时AccessToken验证失败，清除缓存并重试...');
        clearTokenCache();
        return await queryChanjingVideoTaskResult(fastify, videoId, retryCount + 1);
      }
      throw new Error(`${errorMsg} (错误码: ${response.data.code})`);
    }
    const videoData = response.data.data;
    // 处理字幕数据
    let subtitles = [];
    if (videoData.subtitle_data_url) {
      try {


智能任务调度系统 V1.0                                                    第 39 页
--------------------------------------------------------------------------------
        // 下载字幕数据
        const subtitleResponse = await axios.get(videoData.subtitle_data_url);
        subtitles = subtitleResponse.data || [];
        console.log(`成功获取字幕数据，共 ${subtitles.length} 条`);
      } catch (error) {
        console.warn('获取字幕数据失败:', error.message);
      }
    }
    // 返回格式化的视频详情
    return {
      id: videoData.id,
      status: videoData.status,
      progress: videoData.progress,
      msg: videoData.msg || '',
      videoUrl: videoData.video_url,
      previewUrl: videoData.preview_url,
      duration: videoData.duration,
      createTime: videoData.create_time,
      subtitleDataUrl: videoData.subtitle_data_url,
      subtitles: subtitles, // 状态描述
      statusDescription: getVideoStatusDescription(videoData.status), // 是否完成
      isCompleted: videoData.status === 30, // 是否失败
      isFailed: videoData.status >= 40 && videoData.status < 60, // 是否生成中
      isProcessing: videoData.status === 10
    };
  } catch (error) {
    // 如果是网络错误或认证错误，且还有重试次数，尝试重新获取token并重试
    if (retryCount < maxRetries && (error.response?.status === 401 || error.response?.status === 403 || error.message?.includes('AccessToken验证失败') || error.message?.includes('access_token'))) {
      console.log('查询视频详情时检测到认证错误，清除缓存并重试...');
      clearTokenCache();
      return await queryChanjingVideoTaskResult(fastify, videoId, retryCount + 1);
    }
    console.error('查询视频详情失败:', error);
    throw error;
  }
};
const runner = async (fastify, optionsRaw, { task, polling, progress }) => {
  // const host = 'http://18.143.57.170:34570';
  const { question, digital } = task.input;
  const bgUrl = digital.background ? await fastify.fileManager.services.getFileUrl({ id: digital.background }) : null;
  const videoOptions = Object.assign({
    person: {
      figure_type: 'sit_body', // 临时写死了，当前用到数字人形象都是有这个type
      x: 0,
      y: 0,
      width: 1080,
      height: 1920
    },
    audio: {
      tts: {


智能任务调度系统 V1.0                                                    第 40 页
--------------------------------------------------------------------------------
        speed: 1
      },
      type: 'tts',
      wav_url: 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3', // 临时音色
      volume: 100,
      language: languageMap[digital.language || 'cn']
    },
    bg_color: '#EDEDED',
    bg: bgUrl
      ? {
          x: 0,
          y: 0,
          height: 1920,
          width: 1080,
          src_url: bgUrl
        }
      : null,
    subtitle_config: {
      x: 31,
      y: 1521,
      show: false,
      width: 1000,
      height: 200,
      font_size: 64,
      color: '#000000'
    },
    screen_width: 1080,
    screen_height: 1920
  });
  // 构建蝉镜API选项，支持更多配置
  const chanjingOptions = {
    // 数字人配置
    person: {
      x: videoOptions.person?.x,
      y: videoOptions.person?.y,
      width: videoOptions.person?.width,
      height: videoOptions.person?.height,
      figure_type: videoOptions.person?.figure_type || 'whole_body',
      drive_mode: videoOptions.person?.drive_mode,
      is_rgba_mode: videoOptions.person?.is_rgba_mode,
      backway: videoOptions.person?.backway
    }, // 音频配置
    audio: {
      tts: {
        speed: videoOptions.audio?.tts?.speed,
        audio_man: videoOptions.audio?.tts?.audio_man, // 使用数字人的音色ID
        pitch: videoOptions.audio?.tts?.pitch
      },
      wav_url: videoOptions.audio?.wav_url,
      type: videoOptions.audio?.type,


智能任务调度系统 V1.0                                                    第 41 页
--------------------------------------------------------------------------------
      volume: videoOptions.audio?.volume,
      language: languageMap[videoOptions.audio?.language || digital.language || 'cn']
    }, // 背景配置
    bg_color: videoOptions.bg_color,
    bg: videoOptions.bg, // 字幕配置
    subtitle_config: videoOptions.subtitle_config, // 屏幕尺寸
    screen_width: videoOptions.screen_width,
    screen_height: videoOptions.screen_height
  };
  // 调用禅镜API
  const videoGenerationResult = await callChanjingAPI(fastify, {
    text: question.title,
    digital: digital,
    taskId: task.id,
    options: chanjingOptions
  });
  const chanjingTaskId = videoGenerationResult.data;
  //   const chanjingTraceId = videoGenerationResult.trace_id;
  return await polling(async () => {
    try {
      const videoResult = await queryChanjingVideoTaskResult(fastify, chanjingTaskId);
      console.log('蝉镜视频任务查询结果:', videoResult);
      // 根据视频状态更新任务和关联记录
      if (videoResult.isCompleted) {
        // 视频生成成功
        if (!videoResult.videoUrl) {
          throw new Error('视频生成完成但未获取到视频URL');
        }
        console.log(`任务处理成功，视频URL: ${videoResult.videoUrl}`);
        const file = await fastify.fileManager.services.uploadFromUrl({ url: videoResult.videoUrl });
        return { result: 'success', data: Object.assign({}, videoResult, { video: file.id }) };
      } else if (videoResult.isFailed) {
        // 视频生成失败
        const errorMsg = videoResult.msg || `视频生成失败，状态: ${videoResult.statusDescription}`;
        return { result: 'failed', message: errorMsg };
      } else if (videoResult.isProcessing) {
        // TODO: 视频还在生成中
        const progressMsg = `视频生成中，进度: ${videoResult.progress}%`;
        await progress(videoResult.progress);
        console.log('task info:', progressMsg);
        return;
      } else {
        // 其他未知状态
        console.log('task unknown status');
        return;
      }
    } catch (error) {
      console.error('task error: ', error);
      return;
    }


智能任务调度系统 V1.0                                                    第 42 页
--------------------------------------------------------------------------------
  });
};
module.exports = runner;

// 文件: server/libs/tasks/digital/interface1.js
const runner = async (fastify, options, { task, polling, updateProgress }) => {
  const { question, digital } = task.input;
  const videoUrl = await fastify.fileManager.services.getFileUrl({ id: digital.waitingVideo });
  const backgroundUrl = await fastify.fileManager.services.getFileUrl({ id: digital.background });
  const response = await fetch(`${fastify.config.TASK_DIGITAL_HOST}/api/cj/avatar_video`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      text: question.title,
      language: (digital.language || '').toLowerCase().indexOf('cn') > -1 ? 'cn' : 'en',
      gender: digital.options.gender || 'female',
      width: digital.options.sizeType === 'horizontal' ? 1600 : 900,
      height: digital.options.sizeType === 'horizontal' ? 900 : 1600,
      original_video_url: videoUrl,
      background_url: backgroundUrl
    })
  });
  if (!response.ok) {
    throw new Error(`${String(response.status)}:${await response.text()}`);
  }
  const responseData = await response.json();
  if (responseData.status_code !== 200) {
    throw new Error(responseData.message || responseData.msg);
  }
  const videoId = responseData.data.video_id;
  return await polling(async () => {
    const response = await fetch(`${fastify.config.TASK_DIGITAL_HOST}/api/cj/avatar_video_detail/${videoId}`, {
      method: 'GET'
    });
    if (!response.ok) {
      return;
    }
    const responseData = await response.json();
    if (responseData.status_code !== 200 || String(responseData.data.status) === '40') {
      return { result: 'failed', message: responseData.data?.msg || responseData.message || responseData.msg };
    }
    if (String(responseData.data.status) === '30') {
      const file = await fastify.fileManager.services.uploadFromUrl({
        url: responseData.data.video_url
      });
      return { result: 'success', data: Object.assign({}, responseData.data, { video: file.id }) };
    }
  });


智能任务调度系统 V1.0                                                    第 43 页
--------------------------------------------------------------------------------
};
module.exports = runner;

// 文件: server/libs/tasks/probe/interface2.js
const qs = require('qs');
const runner = async (fastify, options, { task, polling }) => {
  const { userId, title, language, probeQuestionsNumber, existingProbeQuestions } = task.input;
  const url = 'https://api.coze.cn/v3/chat';
  const headers = {
    Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
    'Content-Type': 'application/json'
  };
  const body = {
    bot_id: '7538380545018232858',
    user_id: userId,
    stream: false,
    additional_messages: [
      {
        content: JSON.stringify({
          INTERVIEW_QUESTION: title,
          LANGUAGE: language,
          PROBE_QUESTIONS_NUMBER: probeQuestionsNumber,
          EXISTING_PROBE_QUESTIONS: existingProbeQuestions
        }),
        content_type: 'text',
        role: 'user',
        type: 'question'
      }
    ],
    parameters: {}
  };
  const response = await fetch(url, {
    method: 'POST',
    headers: headers,
    body: JSON.stringify(body)
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  if (!response.ok) {
    await task.update({
      status: 'failed',
      error: response.status
    });
    return;
  }
  const resData = await response.json();
  if (resData.code !== 0) {
    await task.update({
      status: 'failed',


智能任务调度系统 V1.0                                                    第 44 页
--------------------------------------------------------------------------------
      error: resData.msg
    });
    return;
  }
  return await polling(async () => {
    const pollResponse = await fetch(
      `${url}/retrieve?${qs.stringify({
        conversation_id: resData.data?.conversation_id,
        chat_id: resData.data?.id
      })}`,
      {
        method: 'GET',
        headers: headers
      }
    );
    if (!pollResponse.ok) {
      return;
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      const resultResponse = await fetch(
        `${url}/message/list?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',
          headers: headers
        }
      );
      if (!resultResponse.ok) {
        return { result: 'failed', message: resultResponse.status };
      }
      const resultResData = await resultResponse.json();
      console.log('resultResData--------------', resultResData);
      if (resultResData.code !== 0) {
        return { result: 'failed', message: resultResData };
      }
      const probeQuestions = (JSON.parse(resultResData.data[0].content)?.ProbesQuestions || []).map(({ QuestionContent, ...question }) => ({ ...question, title: QuestionContent }));
      return { result: 'success', data: probeQuestions };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', message: pollData.msg || '任务处理失败' };
    }
  });
};
module.exports = runner;

// 文件: server/libs/tasks/probe/interface1.js
const runner = async (fastify, options, { task, polling }) => {


智能任务调度系统 V1.0                                                    第 45 页
--------------------------------------------------------------------------------
  const host = fastify.config.TASK_FLOW_UP_HOST;
  const { title, language } = task.input;
  const response = await fetch(`${host}/api/v1/generate/create`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      question: title,
      language
    })
  });
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  const data = await response.json();
  if (data.code !== 0) {
    throw new Error(data);
  }
  const taskId = data.data.tid;
  return await polling(async () => {
    const pollResponse = await fetch(`${host}/api/v1/generate/result?tid=${taskId}`, {
      method: 'GET'
    });
    if (!pollResponse.ok) {
      return {};
    }
    const pollData = await pollResponse.json();
    if (pollData.code === 0 && pollData.data.status === 'completed') {
      return { result: 'success', data: pollData.data.followup };
    }
    if (pollData.code !== 0 || pollData.data.status === 'failed') {
      return { result: 'failed', data: pollData.message };
    }
  });
};
module.exports = runner;

// 文件: server/libs/services/task.js
const fp = require('fastify-plugin');
const get = require('lodash/get');
const fs = require('fs-extra');
const path = require('node:path');
const languageMap = {
  'en-US': 'en',
  'zh-CN': 'cn',
  en: 'en',
  cn: 'cn'
};
module.exports = fp(async (fastify, options) => {


智能任务调度系统 V1.0                                                    第 46 页
--------------------------------------------------------------------------------
  const { models, services } = fastify[options.name];
  const { Op } = fastify.sequelize.Sequelize;
  // 处理系统任务的函数
  const processSystemTask = async task => {
    if (task.status !== 'pending') {
      return;
    }
    const taskModulePath = path.resolve(process.cwd(), 'libs', 'tasks', task.type, `${options.remoteTask}.js`);
    if (!(await fs.exists(taskModulePath))) {
      await task.update({
        status: 'failed',
        error: '未匹配到任务执行器'
      });
    }
    await task.update({
      status: 'running',
      progress: 0
    });
    require(taskModulePath)(fastify, options, {
      task,
      callback: result => {}
    }).catch(error => {
      return task.update({
        status: 'failed',
        error: error.toString()
      });
    });
    try {
      // 目前只处理数字人视频生成任务
      if (task.type === 'digital' && task.status === 'pending') {
        const { question, digital: originalDigital } = task.input;
        const digital = Object.assign({}, originalDigital, {
          language: languageMap[originalDigital.language || 'zh-CN']
        });
        // 更新任务状态为运行中
        await task.update({
          status: 'running',
          progress: 5,
          msg: '正在准备生成视频...'
        });
        fastify.log.info(`开始自动处理系统任务 ID: ${task.id}, 类型: ${task.type}, 问题: "${question.title}", 数字人: "${digital.name}"`);
        // 调用禅镜API生成视频
        try {
          const bgUrl = digital.background ? await fastify.fileManager.services.getFileUrl({ id: digital.background }) : null;
          // 构建蝉镜API选项，根据最新文档修正参数
          const chanjingOptions = {
            person: {
              figure_type: 'sit_body', // 临时写死了，当前用到数字人形象都是有这个type
              x: 0,
              y: 0,


智能任务调度系统 V1.0                                                    第 47 页
--------------------------------------------------------------------------------
              width: 1080,
              height: 1920
            },
            audio: {
              tts: {
                speed: 1
              },
              type: 'tts',
              wav_url: 'https://res.chanjing.cc/chanjing/res/upload/tts/2025-07-24/4b96453e923149d458b431651dda5083.mp3', // 临时音色
              volume: 100,
              language: digital.language
            },
            bg_color: '#EDEDED',
            bg: bgUrl
              ? {
                  x: 0,
                  y: 0,
                  height: 1920,
                  width: 1080,
                  src_url: bgUrl
                }
              : null,
            subtitle_config: {
              x: 31,
              y: 1521,
              show: false,
              width: 1000,
              height: 200,
              font_size: 64,
              color: '#000000'
            },
            screen_width: 1080,
            screen_height: 1920
          };
          // 调用禅镜API
          const videoGenerationResult = await services.question.callChanjingAPI({
            text: question.title,
            digital: digital,
            taskId: task.id,
            options: chanjingOptions
          });
          // 更新任务状态
          await task.update({
            progress: 10,
            msg: '视频生成任务已提交，等待处理...',
            options: {
              ...task.options,
              chanjingTaskId: videoGenerationResult.data,
              chanjingTraceId: videoGenerationResult.trace_id
            }


智能任务调度系统 V1.0                                                    第 48 页
--------------------------------------------------------------------------------
          });
          fastify.log.info(`系统任务 ${task.id} 已提交到禅镜API，任务ID: ${videoGenerationResult.data}`);
          return true;
        } catch (error) {
          // 更新任务状态为失败
          await task.update({
            status: 'failed',
            progress: 0,
            msg: `视频生成失败: ${error.message}`
          });
          fastify.log.error(`系统任务 ${task.id} 处理失败:`, error);
          return false;
        }
      }
      // 处理报告生成任务
      if (task.type === 'report' && task.status === 'pending') {
        // 这里调用报告生成API
        await task.update({
          status: 'running',
          progress: 0
        });
        //这里不阻塞后面的任务执行
        require(`../plugins/report/${options.remoteTask}`)(fastify, options, {
          task,
          callback: async report => {
            const { clientUserId } = task.input;
            const clientUser = await models.clientUser.findByPk(clientUserId);
            clientUser.report = report;
            clientUser.status = 'ended';
            await clientUser.save();
          }
        });
      }
    } catch (error) {
      await task.update({
        status: 'failed',
        progress: 0,
        msg: `任务处理失败: ${error.toString()}`
      });
    }
  };
  // runnerType: system or manual
  // generateProbe: 是否同时生成追问问题数字人
  const createDigital = async (authenticatePayload, { digitalId, questionIds, runnerType, generateProbe }) => {
    const { id: userId } = authenticatePayload;
    const digital = await services.digital.detail({ id: digitalId });
    // 如果需要生成追问，查询所有主问题的追问问题ID
    let allProbeQuestionIds = [];
    if (generateProbe) {
      const probeQuestions = await models.question.findAll({


智能任务调度系统 V1.0                                                    第 49 页
--------------------------------------------------------------------------------
        where: {
          parentId: {
            [Op.in]: questionIds
          },
          type: 'probe'
        },
        attributes: ['id']
      });
      allProbeQuestionIds = probeQuestions.map(q => q.id);
    }
    // 查询主问题和追问问题的当前任务
    const targetIds = [...questionIds, ...allProbeQuestionIds];
    const currentTasks = await models.task.findAll({
      where: {
        type: 'digital',
        targetId: {
          [Op.in]: targetIds
        },
        status: {
          [Op.in]: ['pending', 'running']
        }
      }
    });
    const result = { cancel: 0, create: 0 };
    for (const questionId of questionIds) {
      const question = await services.question.detail(authenticatePayload, { id: questionId });
      if (!question) {
        continue;
      }
      const needCancel = currentTasks.filter(item => {
        return item.targetId === questionId && get(item, 'input.digital.id') === digitalId;
      });
      if (needCancel.length > 0) {
        for (const item of needCancel) {
          await item.update({ status: 'canceled' });
          result.cancel += 1;
        }
      }
      await fastify.task.services.create({
        type: 'digital',
        targetId: questionId,
        runnerType: runnerType || digital.supply,
        input: {
          name: `${question.title}->${digital.name}`,
          question,
          digital
        },
        userId,
        status: 'pending',
        progress: 0


智能任务调度系统 V1.0                                                    第 50 页
--------------------------------------------------------------------------------
      });
      result.create += 1;
      // 创建新任务
      // const task = await models.task.create({
      //   type: 'digital',
      //   targetId: questionId,
      //   runnerType: runnerType || digital.supply,
      //   input: {
      //     name: `${question.title}->${digital.name}`,
      //     question,
      //     digital
      //   },
      //   userId,
      //   status: 'pending',
      //   progress: 0
      // });
      // result.create += 1;
      // // 如果是系统任务，自动处理
      // if (task.runnerType === 'system') {
      //   // 异步处理系统任务，不阻塞当前请求
      //   processSystemTask(task).catch(err => {
      //     fastify.log.error(`自动处理系统任务 ${task.id} 失败:`, err);
      //   });
      // }
    }
    return result;
  };
  const completeDigital = async (authenticatePayload, { id, video }) => {
    const { id: userId } = authenticatePayload;
    const task = await detail(authenticatePayload, { id });
    const { digital, question } = task.input;
    let target = await models.questionDigital.findOne({
      where: {
        questionId: question.id,
        digitalId: digital.id
      }
    });
    if (!target) {
      target = await models.questionDigital.create({
        video,
        questionId: question.id,
        digitalId: digital.id,
        text: question.title
      });
    } else {
      await target.update({ video, text: question.title });
    }
    await task.update({
      status: 'success',
      completedAt: new Date(),


智能任务调度系统 V1.0                                                    第 51 页
--------------------------------------------------------------------------------
      output: { video, text: question.title },
      completedUserId: userId
    });
    return target;
  };
  // 手动触发，检查chanjingTask视频生成的结果【因为本地调试无法使用callback模式】
  // 手动触发，检查chanjingTask视频生成的结果【因为本地调试无法使用callback模式】
  const checkChanjingDigitalTaskResult = async (authenticatePayload, { id, chanjingTaskId }) => {
    try {
      const { id: userId } = authenticatePayload;
      // 获取任务详情
      const task = await detail(authenticatePayload, { id });
      if (!task) {
        throw new Error('任务不存在或者尚未生成');
      }
      const { digital, question } = task.input;
      // 查找问题数字人关联记录
      let target = await models.questionDigital.findOne({
        where: {
          questionId: question.id,
          digitalId: digital.id
        }
      });
      if (!target) {
        target = await models.questionDigital.create({
          questionId: question.id,
          digitalId: digital.id,
          text: question.title
        });
      }
      // 调用服务查询蝉镜任务结果
      console.log(`开始查询蝉镜视频任务结果，任务ID: ${chanjingTaskId}`);
      const videoResult = await services.question.queryChanjingVideoTaskResult(chanjingTaskId);
      console.log('蝉镜视频任务查询结果:', videoResult);
      // 根据视频状态更新任务和关联记录
      if (videoResult.isCompleted) {
        // 视频生成成功
        if (!videoResult.videoUrl) {
          throw new Error('视频生成完成但未获取到视频URL');
        }
        // 更新问题数字人关联记录
        await target.update({
          video: videoResult.videoUrl,
          text: question.title,
          options: {
            ...target.options,
            chanjingResult: {
              video_url: videoResult.videoUrl,
              preview_url: videoResult.previewUrl,
              duration: videoResult.duration,


智能任务调度系统 V1.0                                                    第 52 页
--------------------------------------------------------------------------------
              subtitle_data_url: videoResult.subtitleDataUrl,
              subtitles: videoResult.subtitles,
              create_time: videoResult.createTime,
              chanjing_task_id: chanjingTaskId,
              status: videoResult.status,
              progress: videoResult.progress
            }
          }
        });
        // 更新任务状态为成功
        await task.update({
          status: 'success',
          progress: 100,
          completedAt: new Date(),
          output: {
            video: videoResult.videoUrl,
            text: question.title,
            duration: videoResult.duration,
            previewUrl: videoResult.previewUrl
          },
          completedUserId: userId,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理成功，视频URL: ${videoResult.videoUrl}`);
        return {
          success: true,
          message: '视频生成成功',
          task: task,
          target: target,
          videoResult: videoResult
        };
      } else if (videoResult.isFailed) {
        // 视频生成失败
        const errorMsg = videoResult.msg || `视频生成失败，状态: ${videoResult.statusDescription}`;
        // 更新任务状态为失败
        await task.update({
          status: 'failed',
          progress: 0,
          msg: errorMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理失败: ${errorMsg}`);
        return {
          success: false,


智能任务调度系统 V1.0                                                    第 53 页
--------------------------------------------------------------------------------
          message: errorMsg,
          task: task,
          target: target,
          videoResult: videoResult
        };
      } else if (videoResult.isProcessing) {
        // 视频还在生成中
        const progressMsg = `视频生成中，进度: ${videoResult.progress}%`;
        // 更新任务进度
        await task.update({
          status: 'running',
          progress: videoResult.progress,
          msg: progressMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 处理中: ${progressMsg}`);
        return {
          success: false,
          message: progressMsg,
          task: task,
          target: target,
          videoResult: videoResult,
          isProcessing: true
        };
      } else {
        // 其他未知状态
        const statusMsg = `视频状态未知: ${videoResult.statusDescription}`;
        // 更新任务状态
        await task.update({
          status: 'running',
          progress: videoResult.progress || 0,
          msg: statusMsg,
          options: {
            ...task.options,
            chanjingResult: videoResult
          }
        });
        console.log(`任务 ${id} 状态未知: ${statusMsg}`);
        return {
          success: false,
          message: statusMsg,
          task: task,
          target: target,
          videoResult: videoResult
        };
      }
    } catch (error) {


智能任务调度系统 V1.0                                                    第 54 页
--------------------------------------------------------------------------------
      console.error('检查蝉镜数字人任务结果失败:', error);
      // 尝试更新任务状态为失败（如果任务存在的话）
      try {
        const task = await models.task.findByPk(id);
        if (task) {
          await task.update({
            status: 'failed',
            progress: 0,
            msg: `检查任务结果失败: ${error.message}`
          });
        }
      } catch (updateError) {
        console.error('更新任务状态失败:', updateError);
      }
      throw error;
    }
  };
  const detail = async (authenticatePayload, { id }) => {
    const task = await models.task.findByPk(id);
    if (!task) {
      throw new Error('任务不存在');
    }
    return task;
  };
  const cancel = async (authenticatePayload, { id }) => {
    const task = await detail(authenticatePayload, { id });
    return await task.update({
      status: 'canceled'
    });
  };
  const list = async (authenticatePayload, { filter, perPage, currentPage }) => {
    const whereQuery = {};
    ['targetId', 'type', 'status', 'runnerType'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = filter[key];
      }
    });
    const { rows, count } = await models.task.findAndCountAll({
      where: Object.assign({}, whereQuery),
      offset: perPage * (currentPage - 1),
      limit: perPage,
      order: [['createdAt', 'DESC']]
    });
    return {
      pageData: rows,
      totalCount: count
    };
  };
  // 处理所有待处理的系统任务
  const processPendingSystemTasks = async () => {


智能任务调度系统 V1.0                                                    第 55 页
--------------------------------------------------------------------------------
    try {
      // 查找所有待处理的系统任务
      const pendingTasks = await models.task.findAll({
        where: {
          runnerType: 'system',
          status: 'pending'
        },
        limit: 10 // 每次处理10个任务，避免过载
      });
      if (pendingTasks.length > 0) {
        fastify.log.info(`找到 ${pendingTasks.length} 个待处理的系统任务`);
        await Promise.all(pendingTasks.map(async task => processSystemTask(task)));
      }
    } catch (error) {
      fastify.log.error('处理待处理系统任务时出错:', error);
    }
  };
  // 设置定时任务，定期检查并处理待处理的系统任务
  const setupTaskProcessor = () => {
    return processPendingSystemTasks().catch(err => {
      fastify.log.error('初始处理系统任务失败:', err);
    });
  };
  // 导出服务
  Object.assign(fastify[options.name].services, {
    task: {
      createDigital,
      completeDigital,
      checkChanjingDigitalTaskResult,
      cancel,
      list,
      detail,
      // 导出内部函数，方便其他服务调用
      processSystemTask,
      processPendingSystemTasks,
      setupTaskProcessor
    }
  });
});

// 文件: server/libs/models/task.js
module.exports = ({ DataTypes, definePrimaryType, options }) => {
  return {
    model: {
      type: {
        type: DataTypes.ENUM('digital', 'probe', 'invite', 'report'),
        comment: '任务类型: digital:数字人问题视频生成, probe:追问问题生成,invite:发送邀请邮件,report:生成报告'
      },
      targetId: definePrimaryType('targetId', {
        comment: '任务目标对象id',


智能任务调度系统 V1.0                                                    第 56 页
--------------------------------------------------------------------------------
        allowNull: false
      }),
      runnerType: {
        type: DataTypes.ENUM('manual', 'system'),
        comment: '任务执行者类型: manual为手动执行, system为系统自动执行',
        defaultValue: 'manual',
        allowNull: false
      },
      delay: {
        type: DataTypes.INTEGER,
        comment: '任务延迟执行时间(秒)',
        defaultValue: 0
      },
      completedAt: {
        type: DataTypes.DATE,
        comment: '任务完成时间'
      },
      input: {
        type: DataTypes.JSON,
        comment: '输入数据'
      },
      output: {
        type: DataTypes.JSON,
        comment: '输出数据'
      },
      error: {
        type: DataTypes.JSON,
        comment: '错误信息'
      },
      status: {
        type: DataTypes.ENUM('pending', 'running', 'success', 'failed', 'canceled'),
        comment: '任务状态',
        defaultValue: 'pending'
      },
      progress: {
        type: DataTypes.INTEGER,
        comment: '任务进度(0-100)',
        defaultValue: 0
      },
      msg: {
        type: DataTypes.TEXT,
        comment: '任务消息或错误信息'
      },
      options: {
        type: DataTypes.JSON,
        comment: '任务扩展选项'
      }
    },
    associate: ({ task, project }) => {
      task.belongsTo(options.getUserModel(), {


智能任务调度系统 V1.0                                                    第 57 页
--------------------------------------------------------------------------------
        foreignKey: 'userId',
        as: 'createdUser',
        comment: '创建人(为空时为系统创建)'
      });
      task.belongsTo(options.getUserModel(), {
        foreignKey: 'completedUserId',
        as: 'completedUser',
        comment: '任务完成人'
      });
      task.belongsTo(project, {
        comment: '所属项目'
      });
    },
    options: {
      comment: '任务'
    }
  };
};

// 文件: src/components/Common/Task/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import React from 'react';
import CompleteDigitalTask from './Actions/CompleteDigitalTask';
const Task = createWithRemoteLoader({
  modules: ['components-admin:Task']
})(({ remoteModules, baseUrl }) => {
  const [Task] = remoteModules;
  return (
    <Task
      baseUrl={baseUrl}
      getManualTaskAction={data => {
        if (data.runnerType === 'manual' && data.status === 'pending' && data.type === 'digital') {
          return CompleteDigitalTask;
        }
      }}
    />
  );
});
export default Task;

// 文件: src/components/Common/Task/Actions/CompleteDigitalTask.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { Button, Flex } from 'antd';
import DigitalView from '../Views/DigitalView';
import QuestionView from '../Views/QuestionView';
import get from 'lodash/get';
const CompleteDigitalTask = createWithRemoteLoader({
  modules: ['components-core:FormInfo', 'components-core:InfoPage', 'components-core:Global@usePreset']
})(({ remoteModules, data, onSuccess, ...props }) => {
  const [FormInfo, InfoPage, usePreset] = remoteModules;


智能任务调度系统 V1.0                                                    第 58 页
--------------------------------------------------------------------------------
  const { useFormModal } = FormInfo;
  const formModal = useFormModal();
  const { Upload } = FormInfo.fields;
  const { apis, ajax } = usePreset();
  return (
    <Button
      {...props}
      onClick={() => {
        formModal({
          title: '完成数字人任务',
          autoClose: true,
          formProps: {
            onSubmit: async formData => {
              const { video } = formData;
              const { id } = data;
              const { data: resData } = await ajax(
                Object.assign({}, apis.task.complete, {
                  data: { id, status: 'success', output: { video } }
                })
              );
              if (resData.code !== 0) {
                return false;
              }
              onSuccess && onSuccess();
            }
          },
          children: (
            <Flex vertical gap={12}>
              <InfoPage.Part title="数字人">
                <DigitalView data={Object.assign({}, get(data, 'input.digital'))} />
              </InfoPage.Part>
              <InfoPage.Part title="问题">
                <QuestionView data={Object.assign({}, get(data, 'input.question'))} />
              </InfoPage.Part>
              <FormInfo title="需要完善信息" list={[<Upload name="video" label="数字人视频" rule="REQ" maxLength={1} accept={['video/*']} interceptor={['photo-string', 'array-single']} block />]} />
            </Flex>
          )
        });
      }}
    />
  );
});
export default CompleteDigitalTask;

// 文件: src/components/Common/Task/Views/QuestionView.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const View = createWithRemoteLoader({
  modules: ['components-core:InfoPage@CentralContent', 'components-core:StateTag@StateTagEnum', 'components-core:Image', 'components-core:File@FileLink']
})(({ remoteModules, data }) => {
  const [CentralContent, StateTagEnum] = remoteModules;


智能任务调度系统 V1.0                                                    第 59 页
--------------------------------------------------------------------------------
  return (
    <CentralContent
      dataSource={data}
      columns={[
        {
          name: 'title',
          title: '问题',
          block: true
        },
        {
          name: 'determinant',
          title: '判断依据',
          block: true
        },
        {
          name: 'language',
          title: '语言',
          render: value => <StateTagEnum moduleName="language" name={value} />
        },
        {
          name: 'preparationTime',
          title: '准备时间',
          render: value => `${value}s`
        },
        {
          name: 'answerMinTime',
          title: '答题最短时间',
          render: value => `${value}s`
        },
        {
          name: 'answerMaxTime',
          title: '答题最长时间',
          render: value => `${value}s`
        },
        {
          name: 'hasProbe',
          title: '是否开启追问',
          valueIsEmpty: () => false,
          render: value => (value ? '是' : '否')
        },
        {
          name: 'group',
          title: '分组'
        }
      ]}
    />
  );
});
export default View;



智能任务调度系统 V1.0                                                    第 60 页
--------------------------------------------------------------------------------
// 文件: src/components/Common/Task/Views/index.js
export { default as QuestionView } from './QuestionView';
export { default as DigitalView } from './DigitalView';

// 文件: src/components/Common/Task/Views/DigitalView.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const View = createWithRemoteLoader({
  modules: ['components-core:InfoPage@CentralContent', 'components-core:StateTag@StateTagEnum', 'components-core:Image', 'components-core:File@FileLink']
})(({ remoteModules, data }) => {
  const [CentralContent, StateTagEnum, Image, FileLink] = remoteModules;
  return (
    <CentralContent
      dataSource={data}
      columns={[
        {
          name: 'name',
          title: '名称'
        },
        {
          name: 'language',
          title: '语言',
          render: value => <StateTagEnum moduleName="language" name={value} />
        },
        {
          name: 'cover',
          title: '封面',
          render: item => {
            return <Image id={item} />;
          }
        },
        {
          name: 'background',
          title: '背景图',
          render: item => {
            return <Image id={item} />;
          }
        },
        {
          name: 'waitingVideo',
          title: '等待视频',
          render: item => {
            return <FileLink id={item}>预览</FileLink>;
          }
        }
      ]}
    />
  );
});
export default View;


