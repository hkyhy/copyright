# AI智能面试题目生成系统 源代码文档


## 前30页


AI智能面试题目生成系统 V1.0                                                    第 1 页
--------------------------------------------------------------------------------
// 文件: server/libs/services/question.js
const fp = require('fastify-plugin');
const transform = require('lodash/transform');
const qs = require('qs');
const languageMap = {
  'en-US': 'en',
  'zh-CN': 'cn',
  en: 'en',
  cn: 'cn'
};
module.exports = fp(async (fastify, options) => {
  const { models, services } = fastify[options.name];
  const { Op } = fastify.sequelize.Sequelize;
  const batchCreate = async (authenticatePayload, { list, ...other }) => {
    const { id: userId } = authenticatePayload;
    if (!(list && list.length > 0)) {
      return [];
    }
    return await models.question.bulkCreate(list.map(item => Object.assign({}, item, other, { userId })));
  };
  const create = async (authenticatePayload, data) => {
    const { id: userId } = authenticatePayload;
    const question = await models.question.create({ ...data, userId });
    if (question.hasProbe) {
      fastify.task.services.create({
        type: 'probe',
        runnerType: 'system',
        input: {
          id: question.id,
          name: question.title,
          title: question.title,
          language: question.language,
          userId,
          preparationTime: question.preparationTime,
          answerType: question.answerType,
          answerMaxTime: question.answerMaxTime,
          answerMinTime: question.answerMinTime,
          clientId: question?.clientId || null
        },
        targetId: question.id,
        targetType: 'question',
        userId
      });
    }
    return question;
  };
  const getJobQuestions = async (authenticatePayload, { jobDescription, language, questionCount }) => {
    const { id: userId } = authenticatePayload;
    const url = 'https://api.coze.cn/v3/chat';
    const headers = {


AI智能面试题目生成系统 V1.0                                                    第 2 页
--------------------------------------------------------------------------------
      Authorization: `Bearer ${process.env.COZ_AUTHORIZATION}`,
      'Content-Type': 'application/json'
    };
    const body = {
      bot_id: '7538365198047100928',
      user_id: userId,
      stream: false,
      additional_messages: [
        {
          content: JSON.stringify({
            JOB_DESCRIPTION: jobDescription,
            LANGUAGE: language,
            QUESTION_COUNT: questionCount
          }),
          content_type: 'text',
          role: 'user',
          type: 'question'
        }
      ],
      parameters: {}
    };
    const response = await fetch(url, {
      method: 'POST',
      headers: headers,
      body: JSON.stringify(body)
    });
    if (!response.ok) {
      throw new Error('生成失败');
    }
    const resData = await response.json();
    // 轮询配置
    const maxRetries = 60; // 最多重试60次
    const retryInterval = 2000; // 每2秒重试一次
    let retryCount = 0;
    // 轮询等待任务完成
    while (retryCount < maxRetries) {
      // 查询任务状态
      const pollResponse = await fetch(
        `${url}/retrieve?${qs.stringify({
          conversation_id: resData.data?.conversation_id,
          chat_id: resData.data?.id
        })}`,
        {
          method: 'GET',
          headers: headers
        }
      );
      const pollData = await pollResponse.json();
      // 如果任务完成，获取消息列表
      if (pollData.code === 0 && pollData.data.status === 'completed') {


AI智能面试题目生成系统 V1.0                                                    第 3 页
--------------------------------------------------------------------------------
        const resultResponse = await fetch(
          `${url}/message/list?${qs.stringify({
            conversation_id: resData.data?.conversation_id,
            chat_id: resData.data?.id
          })}`,
          {
            method: 'GET',
            headers: headers
          }
        );
        if (!resultResponse.ok) {
          throw new Error('获取结果失败');
        }
        const resultResData = await resultResponse.json();
        if (resultResData.code !== 0) {
          throw new Error(resultResData.msg || '获取结果失败');
        }
        console.log('生成完成，返回数据:', resultResData.data);
        return JSON.parse(resultResData.data[0].content);
      }
      // 如果任务失败，抛出错误
      if (pollData.code !== 0 || pollData.data?.status === 'failed') {
        throw new Error(pollData.msg || '生成失败');
      }
      // 如果任务还在进行中，继续轮询
      if (pollData.data?.status === 'in_progress') {
        console.log(`任务进行中，第 ${retryCount + 1} 次轮询...`);
        await new Promise(resolve => setTimeout(resolve, retryInterval));
        retryCount++;
      } else {
        // 未知状态，抛出错误
        throw new Error(`未知任务状态: ${pollData.data?.status}`);
      }
    }
    // 超过最大重试次数
    throw new Error('生成超时，请稍后重试');
  };
  const jdBatchCreate = async (authenticatePayload, { clientId = null, jobDescription, language, questionCount }) => {
    const { id: userId } = authenticatePayload;
    const { questionList } = await getJobQuestions(authenticatePayload, { jobDescription, language, questionCount });
    const questionMap = new Map((questionList || []).map(item => [item.mainQuestionContent, item.probeQuestions || []]));
    const questions = await services.question.batchCreate(
      { id: userId },
      {
        list: (questionList || [])?.map(item => ({ ...item, title: item.mainQuestionContent })),
        preparationTime: 30,
        answerType: 'video',
        type: 'main',
        userId,
        hasProbe: true,


AI智能面试题目生成系统 V1.0                                                    第 4 页
--------------------------------------------------------------------------------
        clientId,
        jobDescription
      }
    );
    for (const question of questions) {
      const title = question.title;
      const probeQuestions = questionMap.get(title);
      await services.question.batchCreate(
        { id: userId },
        {
          list: (probeQuestions || [])?.map(item => ({ ...item, title: item.questionContent })),
          preparationTime: 30,
          answerType: 'video',
          type: 'probe',
          userId,
          hasProbe: false,
          clientId,
          jobDescription,
          parentId: question.id
        }
      );
    }
    return questions;
  };
  const list = async (authenticatePayload, { filter, perPage, currentPage, clientId }) => {
    const whereQuery = {
      type: 'main'
    };
    if (clientId) {
      whereQuery.clientId = clientId;
    }
    ['id', 'language', 'hasProbe', 'parentId', 'type'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = key === 'hasProbe' ? JSON.parse(filter[key].toLowerCase()) : filter[key];
      }
    });
    ['title'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = {
          [Op.like]: `%${filter[key]}%`
        };
      }
    });
    const { count, rows } = await models.question.findAndCountAll({
      where: Object.assign({}, whereQuery),
      include: [
        {
          model: models.client,
          required: false,
          attributes: ['id', 'name']


AI智能面试题目生成系统 V1.0                                                    第 5 页
--------------------------------------------------------------------------------
        }
      ],
      offset: perPage * (currentPage - 1),
      limit: perPage,
      order: [['createdAt', 'DESC']]
    });
    const questionDigitalList = await models.questionDigital.findAll({
      include: [models.digital],
      where: {
        questionId: {
          [Op.in]: rows.map(item => item.id)
        }
      }
    });
    const mapping = transform(
      questionDigitalList,
      (result, value) => {
        if (!result[value.questionId]) {
          result[value.questionId] = [];
        }
        result[value.questionId].push(value);
      },
      {}
    );
    const tasks = await fastify.task.models.task.findAll({
      where: {
        type: 'digital',
        targetType: 'question',
        targetId: {
          [Op.in]: rows.map(item => item.id)
        },
        status: {
          [Op.in]: ['pending', 'running']
        }
      },
      attributes: ['targetId', [fastify.sequelize.Sequelize.fn('COUNT', 'targetId'), 'count']],
      group: ['targetId']
    });
    const tasksMapping = transform(
      tasks,
      (result, item) => {
        const value = item.get({ pain: true });
        result[value.targetId] = Number(value.count);
      },
      {}
    );
    return {
      pageData: rows.map(item => {
        return Object.assign({}, item.get({ pain: true }), {
          questionDigitals: mapping[item.id] || [],


AI智能面试题目生成系统 V1.0                                                    第 6 页
--------------------------------------------------------------------------------
          taskCount: tasksMapping[item.id] || 0
        });
      }),
      tasksMapping,
      tasks,
      totalCount: count
    };
  };
  const listByDigital = async (authenticatePayload, { digitalId, filter, perPage, currentPage }) => {
    const whereQuery = {};
    ['id', 'hasProbe', 'parentId', 'type'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = key === 'hasProbe' ? JSON.parse(filter[key].toLowerCase()) : filter[key];
      }
    });
    ['title'].forEach(key => {
      if (filter && filter[key]) {
        whereQuery[key] = {
          [Op.like]: `%${filter[key]}%`
        };
      }
    });
    const { count, rows } = await models.questionDigital.findAndCountAll({
      include: [
        {
          model: models.digital,
          where: { id: digitalId }
        },
        {
          model: models.question,
          where: Object.assign({}, whereQuery)
        }
      ],
      offset: perPage * (currentPage - 1),
      limit: perPage
    });
    return {
      pageData: rows,
      totalCount: count
    };
  };
  const detail = async (authenticatePayload, { id }) => {
    const question = await models.question.findByPk(id);
    if (!question) {
      throw new Error('question not found');
    }
    return question;
  };
  const save = async (authenticatePayload, { id, ...data }) => {
    const question = await detail(authenticatePayload, { id });


AI智能面试题目生成系统 V1.0                                                    第 7 页
--------------------------------------------------------------------------------
    return question.update(data);
  };
  const remove = async (authenticatePayload, { id }) => {
    const question = await detail(authenticatePayload, { id });
    // 检查question是否关联了开启状态的project
    const projectQuestions = await models.projectQuestion.findAll({
      include: [
        {
          model: models.project,
          where: { status: 'open' }
        },
        {
          model: models.questionDigital,
          where: { questionId: id }
        }
      ]
    });
    if (projectQuestions.length > 0) {
      throw new Error('无法删除问题，存在关联的开启状态项目');
    }
    // 删除关联的项目问题记录（只删除关闭状态项目的关联）
    await models.projectQuestion.destroy({
      where: {
        id: {
          [Op.in]: projectQuestions.map(item => item.id)
        }
      }
    });
    // 删除关联的问题数字人记录
    await models.questionDigital.destroy({
      where: { questionId: id }
    });
    // 删除问题本身
    await question.destroy();
  };
  const generateDigital = async (authenticatePayload, { digitalId, questionIds, generateProbeDigital, reGenerateDigital }) => {
    const digital = await services.digital.detail({ id: digitalId });
    // 如果 reGenerateDigital 为 false，先检查是否所有题目都已有数字人视频
    if (!reGenerateDigital) {
      let allExist = true;
      const checkResults = [];
      for (const questionId of questionIds) {
        const question = await detail(authenticatePayload, { id: questionId });
        // 检查主问题
        const existingDigital = await models.questionDigital.findOne({
          where: {
            questionId: questionId,
            digitalId: digitalId
          }
        });


AI智能面试题目生成系统 V1.0                                                    第 8 页
--------------------------------------------------------------------------------
        const mainQuestionHasVideo = existingDigital && existingDigital.video && existingDigital.video !== '';
        if (!mainQuestionHasVideo) {
          allExist = false;
        }
        // 如果需要生成追问数字人，也要检查追问问题
        if (generateProbeDigital && question.hasProbe && allExist) {
          const probeQuestions = await models.question.findAll({
            where: {
              parentId: questionId,
              type: 'probe',
              deletedAt: null
            }
          });
          for (const probeQuestion of probeQuestions) {
            const existingProbeDigital = await models.questionDigital.findOne({
              where: {
                questionId: probeQuestion.id,
                digitalId: digitalId
              }
            });
            const probeHasVideo = existingProbeDigital && existingProbeDigital.video && existingProbeDigital.video !== '';
            if (!probeHasVideo) {
              allExist = false;
              break;
            }
          }
        }
        if (!allExist) {
          break;
        }
      }
      // 如果所有题目都已有视频，返回提示信息
      if (allExist) {
        return {
          success: false,
          message: `所选题目${generateProbeDigital ? '及其追问' : ''}均已生成【${digital.name}】数字人视频，无需重复生成。如需重新生成，请勾选"覆盖已有的数字人"选项。`
        };
      }
    }
    // 执行生成逻辑
    let generatedCount = 0;
    let skippedCount = 0;
    for (const questionId of questionIds) {
      const question = await detail(authenticatePayload, { id: questionId });
      // 检查主问题是否已经生成过当前数字人的视频
      const existingDigital = await models.questionDigital.findOne({
        where: {
          questionId: questionId,
          digitalId: digitalId
        }


AI智能面试题目生成系统 V1.0                                                    第 9 页
--------------------------------------------------------------------------------
      });
      // 判断是否需要生成主问题的数字人视频
      const needGenerate = reGenerateDigital || !existingDigital || !existingDigital.video || existingDigital.video === '';
      if (needGenerate) {
        console.log(`[generateDigital] ${reGenerateDigital ? '重新生成' : '生成'}主问题数字人视频: ${question.title}`);
        // 取消旧任务
        await fastify.task.services.cancel({
          targetId: questionId,
          targetType: 'question',
          type: 'digital'
        });
        // 创建新任务
        await fastify.task.services.create({
          runnerType: digital.supply,
          input: {
            name: `${question.title}->${digital.name}`,
            question,
            digital: Object.assign({}, digital.toJSON(), {
              language: languageMap[digital.language]
            })
          },
          targetId: questionId,
          targetType: 'question',
          userId: authenticatePayload.id,
          type: 'digital'
        });
        generatedCount++;
      } else {
        console.log(`[generateDigital] 主问题已存在数字人视频，跳过: ${question.title}`);
        skippedCount++;
      }
      // 如果开启了生成追问数字人，并且问题包含追问
      if (generateProbeDigital && question.hasProbe) {
        // 查找该问题的所有追问问题
        const probeQuestions = await models.question.findAll({
          where: {
            parentId: questionId,
            type: 'probe',
            deletedAt: null
          }
        });
        console.log(`[generateDigital] 问题 ${question.title} 包含 ${probeQuestions.length} 个追问问题`);
        // 为每个追问问题创建数字人任务
        for (const probeQuestion of probeQuestions) {
          // 检查追问问题是否已经生成过当前数字人的视频
          const existingDigital = await models.questionDigital.findOne({
            where: {
              questionId: probeQuestion.id,
              digitalId: digitalId
            }


AI智能面试题目生成系统 V1.0                                                    第 10 页
--------------------------------------------------------------------------------
          });
          // 判断是否需要生成追问问题的数字人视频
          // 1. reGenerateDigital 为 true，总是生成
          // 2. 没有记录，需要生成
          // 3. 有记录但没有视频，需要生成
          // 4. 有记录且有视频，根据 reGenerateDigital 决定
          const needGenerate = reGenerateDigital || !existingDigital || !existingDigital.video || existingDigital.video === '';
          if (needGenerate) {
            console.log(`[generateDigital] ${reGenerateDigital ? '重新生成' : '生成'}追问问题数字人视频: ${probeQuestion.title}`);
            // 取消可能存在的旧任务
            await fastify.task.services.cancel({
              targetId: probeQuestion.id,
              targetType: 'question',
              type: 'digital'
            });
            // 创建新的数字人任务
            await fastify.task.services.create({
              runnerType: digital.supply,
              input: {
                name: `${probeQuestion.title}->${digital.name}`,
                question: probeQuestion.toJSON(),
                digital: Object.assign({}, digital.toJSON(), {
                  language: languageMap[digital.language]
                })
              },
              targetId: probeQuestion.id,
              targetType: 'question',
              userId: authenticatePayload.id,
              type: 'digital'
            });
            generatedCount++;
          } else {
            console.log(`[generateDigital] 追问问题已存在数字人视频，跳过: ${probeQuestion.title}`);
            skippedCount++;
          }
        }
      }
    }
    // 返回生成结果
    return {
      success: true,
      message: `成功创建 ${generatedCount} 个数字人任务${skippedCount > 0 ? `，跳过 ${skippedCount} 个已存在的视频` : ''}`,
      generatedCount,
      skippedCount
    };
  };
  const generateProbe = async (authenticatePayload, { probeQuestionsNumber, questionIds }) => {
    for (const questionId of questionIds) {
      await fastify.task.services.cancel({
        targetId: questionId,


AI智能面试题目生成系统 V1.0                                                    第 11 页
--------------------------------------------------------------------------------
        targetType: 'question',
        type: 'probe'
      });
      const probeQuestions = await models.question.findAll({
        where: {
          parentId: questionId,
          type: 'probe'
        },
        attributes: ['title']
      });
      const question = await detail(authenticatePayload, { id: questionId });
      await fastify.task.services.create({
        type: 'probe',
        runnerType: 'system',
        input: {
          id: question.id,
          name: question.title,
          title: question.title,
          language: question.language,
          userId: authenticatePayload.id,
          preparationTime: question.preparationTime,
          answerType: question.answerType,
          answerMaxTime: question.answerMaxTime,
          answerMinTime: question.answerMinTime,
          clientId: question?.clientId || null,
          existingProbeQuestions: probeQuestions.map(item => item.title),
          probeQuestionsNumber
        },
        targetId: question.id,
        targetType: 'question',
        userId: authenticatePayload.id
      });
    }
  };
  const digitalList = async (authenticatePayload, { id, perPage, currentPage }) => {
    const question = await detail(authenticatePayload, { id });
    const { count, rows } = await models.questionDigital.findAndCountAll({
      include: [models.digital, models.question],
      where: { questionId: question.id },
      offset: perPage * (currentPage - 1),
      limit: perPage,
      order: [['createdAt', 'DESC']]
    });
    return {
      pageData: rows,
      totalCount: count
    };
  };
  const excelInclude = () => {};
  const excelExport = () => {};


AI智能面试题目生成系统 V1.0                                                    第 12 页
--------------------------------------------------------------------------------
  const probeTask = async ({ result, task }) => {
    const { userId, language, preparationTime, answerType, answerMaxTime, answerMinTime, probeQuestionsNumber } = task.input;
    console.log('result-----------', result);
    const props = {
      list: result?.map(item => (typeof item === 'object' ? item : { title: item })),
      parentId: task.targetId,
      language,
      preparationTime,
      answerType,
      type: 'probe'
    };
    if (!probeQuestionsNumber) {
      props.answerMaxTime = answerMaxTime;
      props.answerMinTime = answerMinTime;
    }
    await services.question.batchCreate({ id: userId }, props);
  };
  const digitalTask = async ({ result, task }) => {
    const { digital, question } = task.input;
    let target = await models.questionDigital.findOne({
      where: {
        questionId: question.id,
        digitalId: digital.id
      }
    });
    if (!target) {
      await models.questionDigital.create({
        video: result.video,
        questionId: question.id,
        digitalId: digital.id,
        text: question.title
      });
    } else {
      await target.update({ video: result.video, text: question.title });
    }
  };
  Object.assign(fastify[options.name].services, {
    question: {
      create,
      jdBatchCreate,
      batchCreate,
      list,
      detail,
      save,
      remove,
      digitalList,
      generateDigital,
      generateProbe,
      excelInclude,
      excelExport,


AI智能面试题目生成系统 V1.0                                                    第 13 页
--------------------------------------------------------------------------------
      probeTask,
      digitalTask,
      listByDigital
    }
  });
});

// 文件: server/libs/controllers/admin-question.js
const fp = require('fastify-plugin');
const merge = require('lodash/merge');
const questionSchema = require('../schema/question.schema');
module.exports = fp(async (fastify, options) => {
  const { services } = fastify[options.name];
  const { authenticate } = fastify.account;
  fastify.post(
    `${options.prefix}/admin/question/create`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '添加问题',
        body: Object.assign({}, questionSchema)
      }
    },
    async request => {
      return services.question.create(request.userInfo, request.body);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/jd-batch-create`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '根据JD添加问题',
        body: {
          type: 'object',
          properties: {
            language: {
              type: 'string',
              enum: ['zh-CN', 'en-US'],
              default: 'zh-CN',
              description: '语言'
            },
            hasProbe: {
              type: 'boolean',
              description: '是否包含追问问题'
            },
            options: {
              type: 'object',
              description: '扩展字段'
            },


AI智能面试题目生成系统 V1.0                                                    第 14 页
--------------------------------------------------------------------------------
            jobDescription: {
              type: 'string',
              description: 'JD描述'
            },
            questionCount: {
              type: 'number',
              description: '默认生成数量'
            }
          }
        }
      }
    },
    async request => {
      return services.question.jdBatchCreate(request.userInfo, request.body);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/list`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题列表',
        query: {
          type: 'object',
          properties: {
            filter: {
              type: 'object'
            },
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            }
          }
        }
      }
    },
    async request => {
      return services.question.list(request.userInfo, request.query);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/list-digital`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题数字人列表',


AI智能面试题目生成系统 V1.0                                                    第 15 页
--------------------------------------------------------------------------------
        query: {
          type: 'object',
          properties: {
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            },
            digitalId: {
              type: 'string'
            },
            filter: {
              type: 'object'
            }
          },
          required: ['digitalId']
        }
      }
    },
    async request => {
      return services.question.listByDigital(request.userInfo, request.query);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/digital-list`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取题目数字人列表',
        query: {
          type: 'object',
          properties: {
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            },
            id: {
              type: 'string'
            }
          },
          required: ['id']
        }
      }


AI智能面试题目生成系统 V1.0                                                    第 16 页
--------------------------------------------------------------------------------
    },
    async request => {
      return services.question.digitalList(request.userInfo, request.query);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/detail`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题详情',
        query: {
          type: 'object',
          properties: {
            id: {
              type: 'string'
            }
          },
          required: ['id']
        }
      }
    },
    async request => {
      return services.question.detail(request.userInfo, request.query);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/save`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '保存问题',
        body: merge({}, questionSchema, {
          required: ['id'],
          properties: {
            id: {
              type: 'string'
            }
          }
        })
      }
    },
    async request => {
      return services.question.save(request.userInfo, request.body);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/remove`,
    {
      onRequest: [authenticate.user, authenticate.admin],


AI智能面试题目生成系统 V1.0                                                    第 17 页
--------------------------------------------------------------------------------
      schema: {
        summary: '删除问题',
        body: {
          type: 'object',
          properties: {
            id: {
              type: 'string'
            }
          }
        }
      }
    },
    async request => {
      await services.question.remove(request.userInfo, request.body);
      return {};
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/generate-digital`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '生成问题数字人',
        body: {
          type: 'object',
          properties: {
            digitalId: {
              type: 'string'
            },
            questionIds: {
              type: 'array',
              items: {
                type: 'string'
              },
              minItems: 1
            },
            generateProbeDigital: {
              type: 'boolean'
            },
            reGenerateDigital: {
              type: 'boolean'
            }
          },
          required: ['digitalId', 'questionIds']
        }
      }
    },
    async request => {
      return await services.question.generateDigital(request.userInfo, request.body);
    }


AI智能面试题目生成系统 V1.0                                                    第 18 页
--------------------------------------------------------------------------------
  );
  fastify.post(
    `${options.prefix}/admin/question/generate-probe`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '生成追问问题',
        body: {
          type: 'object',
          properties: {
            questionIds: {
              type: 'array',
              items: {
                type: 'string'
              },
              minItems: 1
            },
            probeQuestionsNumber: {
              type: 'number'
            }
          },
          required: ['questionIds']
        }
      }
    },
    async request => {
      await services.question.generateProbe(request.userInfo, request.body);
      return {};
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/video-generation-status/:taskId`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '查询视频生成任务状态',
        params: {
          type: 'object',
          properties: {
            taskId: {
              type: 'string'
            }
          },
          required: ['taskId']
        }
      }
    },
    async request => {
      return services.question.getVideoGenerationStatus(request.userInfo, request.params);
    }


AI智能面试题目生成系统 V1.0                                                    第 19 页
--------------------------------------------------------------------------------
  );
});

// 文件: server/libs/models/question.js
module.exports = ({ DataTypes, definePrimaryType, options }) => {
  return {
    model: {
      title: {
        type: DataTypes.STRING,
        allowNull: false,
        comment: '题目'
      },
      type: {
        type: DataTypes.ENUM('main', 'probe'),
        defaultValue: 'main',
        comment: '题目类型: main: 主要问题, probe: 追问问题'
      },
      language: {
        type: DataTypes.ENUM('zh-CN', 'en-US'),
        defaultValue: 'zh-CN',
        comment: '语言'
      },
      preparationTime: {
        type: DataTypes.INTEGER,
        defaultValue: 30,
        comment: '准备时间,单位秒'
      },
      answerType: {
        type: DataTypes.ENUM('single', 'multiple', 'text', 'video'),
        defaultValue: 'video',
        comment: '答案类型: single: 单选, multiple: 多选, text: 文本, video: 视频'
      },
      answerMaxTime: {
        type: DataTypes.INTEGER,
        defaultValue: 60 * 2,
        comment: '答题最长时间,单位秒'
      },
      answerMinTime: {
        type: DataTypes.INTEGER,
        defaultValue: 30,
        comment: '答题最短时间,单位秒'
      },
      determinant: {
        type: DataTypes.TEXT,
        comment: '判定规则(主要提供给模型)'
      },
      parentId: definePrimaryType('parentId', {
        comment: '追问问题关联的主问题ID'
      }),
      groups: {


AI智能面试题目生成系统 V1.0                                                    第 20 页
--------------------------------------------------------------------------------
        type: DataTypes.JSON,
        comment: '分组列表',
        defaultValue: []
      },
      hasProbe: {
        type: DataTypes.BOOLEAN,
        comment: '是否包含追问问题',
        defaultValue: false
      },
      options: {
        type: DataTypes.JSON,
        comment: '扩展字段'
      },
      jobDescription: {
        type: DataTypes.TEXT,
        comment: '职位描述'
      }
    },
    associate: ({ question, client }) => {
      question.belongsTo(options.getUserModel(), {
        comment: '创建人',
        allowNull: false
      });
      question.belongsTo(client, {
        comment: '所属客户',
        allowNull: true
      });
    },
    options: {
      comment: '问题',
      indexes: [
        {
          fields: ['title', 'parent_id', 'deleted_at'],
          unique: true
        }
      ]
    }
  };
};

// 文件: src/components/Admin/Question/index.js
import { Routes, Route } from 'react-router-dom';
import { createWithRemoteLoader } from '@kne/remote-loader';
const Question = createWithRemoteLoader({
  modules: ['ai-interview-v2:Common', 'components-core:Global@usePreset']
})(({ baseUrl, remoteModules }) => {
  const [Common, usePreset] = remoteModules;
  const { List, Detail } = Common.Question;
  const { apis } = usePreset();
  return (


AI智能面试题目生成系统 V1.0                                                    第 21 页
--------------------------------------------------------------------------------
    <Routes>
      <Route index element={<List baseUrl={baseUrl} api={apis.question.list} />} />
      <Route path="detail" element={<Detail baseUrl={baseUrl} api={apis.question.detail} />} />
    </Routes>
  );
});
export default Question;

// 文件: src/components/Common/Question/index.js
import List, { getColumns } from './List';
import { ManualFormInner, JDFormInner } from './FormInner';
import Detail from './Detail';
import Actions from './Actions';
export default {
  List,
  getColumns,
  ManualFormInner,
  JDFormInner,
  Detail,
  Actions
};

// 文件: src/components/Common/Question/VideoGenerationStatus/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useEffect, useState } from 'react';
import { Progress, Typography, Alert, Space } from 'antd';
const VideoGenerationStatus = createWithRemoteLoader({
  modules: ['components-core:Global@usePreset']
})(({ remoteModules, taskId, onComplete, isClient }) => {
  const [usePreset] = remoteModules;
  const { apis, ajax } = usePreset();
  const [status, setStatus] = useState(null);
  const [loading, setLoading] = useState(true);
  const fetchStatus = async () => {
    try {
      const { data: resData } = await ajax(
        Object.assign({}, isClient ? apis.clientAdmin.question.getVideoGenerationStatus : apis.question.getVideoGenerationStatus, {
          urlParams: { taskId }
        })
      );
      if (resData.code === 0) {
        setStatus(resData.data);
        // 如果任务完成，调用回调函数
        if (resData.data.status === 'success' || resData.data.status === 'failed') {
          onComplete && onComplete(resData.data);
        }
      }
    } catch (error) {
      console.error('获取任务状态失败:', error);
    } finally {


AI智能面试题目生成系统 V1.0                                                    第 22 页
--------------------------------------------------------------------------------
      setLoading(false);
    }
  };
  useEffect(() => {
    if (!taskId) return;
    // 立即获取一次状态
    fetchStatus();
    // 如果任务还在进行中，每3秒轮询一次
    const interval = setInterval(() => {
      if (status && (status.status === 'pending' || status.status === 'running')) {
        fetchStatus();
      } else {
        clearInterval(interval);
      }
    }, 3000);
    return () => clearInterval(interval);
  }, [taskId, status?.status]);
  if (loading) {
    return <div>加载中...</div>;
  }
  if (!status) {
    return <Alert message="无法获取任务状态" type="error" />;
  }
  const getStatusColor = status => {
    switch (status) {
      case 'success':
        return 'success';
      case 'failed':
        return 'exception';
      case 'running':
        return 'active';
      default:
        return 'normal';
    }
  };
  const getStatusText = status => {
    switch (status) {
      case 'pending':
        return '等待处理';
      case 'running':
        return '生成中';
      case 'success':
        return '生成成功';
      case 'failed':
        return '生成失败';
      case 'canceled':
        return '已取消';
      default:
        return '未知状态';
    }


AI智能面试题目生成系统 V1.0                                                    第 23 页
--------------------------------------------------------------------------------
  };
  return (
    <Space direction="vertical" style={{ width: '100%' }}>
      <Typography.Text strong>视频生成状态</Typography.Text>
      <Progress percent={status.progress || 0} status={getStatusColor(status.status)} format={() => `${status.progress || 0}%`} />
      <Typography.Text>状态: {getStatusText(status.status)}</Typography.Text>
      {status.msg && <Alert message={status.msg} type={status.status === 'failed' ? 'error' : 'info'} showIcon />}
      {status.status === 'success' && status.options?.chanjingCallback?.video_url && (
        <Alert
          message="视频生成成功！"
          description={
            <div>
              <p>视频已保存到数据库</p>
              <a href={status.options.chanjingCallback.video_url} target="_blank" rel="noopener noreferrer">
                查看生成的视频
              </a>
            </div>
          }
          type="success"
          showIcon
        />
      )}
    </Space>
  );
});
export default VideoGenerationStatus;

// 文件: src/components/Common/Question/FormInner/ManualFormInner.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const ManualFormInner = createWithRemoteLoader({
  modules: ['components-core:FormInfo']
})(({ remoteModules, isProbe, isEdit }) => {
  const [FormInfo] = remoteModules;
  const { TextArea, Select, InputNumber, Switch } = FormInfo.fields;
  return (
    <FormInfo
      list={[
        <TextArea name="title" label="题目" rule="REQ LEN-0-500" disabled={isEdit} />,
        <Select
          disabled={isEdit}
          name="language"
          label="语言"
          rule="REQ"
          options={[
            { label: '中文', value: 'zh-CN' },
            { label: '英文', value: 'en-US' }
          ]}
          defaultValue="zh-CN"
        />,
        <InputNumber name="preparationTime" label="准备时间(秒)" defaultValue={30} precision={0} />,


AI智能面试题目生成系统 V1.0                                                    第 24 页
--------------------------------------------------------------------------------
        <InputNumber name="answerMinTime" label="答题最短时间(秒)" defaultValue={30} precision={0} />,
        <InputNumber name="answerMaxTime" label="答题最长时间(秒)" defaultValue={60 * 2} precision={0} />,
        <Switch name="hasProbe" label="是否开启追问" display={!isProbe} />
      ]}
    />
  );
});
export default ManualFormInner;

// 文件: src/components/Common/Question/FormInner/JDFormInner.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const JDFormInner = createWithRemoteLoader({
  modules: ['components-core:FormInfo']
})(({ remoteModules }) => {
  const [FormInfo] = remoteModules;
  const { TextArea, Select, InputNumber, Switch } = FormInfo.fields;
  return (
    <FormInfo
      column={1}
      list={[
        <TextArea name="jobDescription" label="JD" rule="REQ LEN-0-5000" />,
        <Select
          name="language"
          label="语言"
          rule="REQ"
          options={[
            { label: '中文', value: 'zh-CN' },
            { label: '英文', value: 'en-US' }
          ]}
          defaultValue="zh-CN"
        />,
        <InputNumber name="questionCount" label="生成数量" defaultValue={5} min={1} max={10} rule="REQ" precision={0} />,
        // <InputNumber name="preparationTime" label="准备时间(秒)" defaultValue={30} disabled precision={0} />,
        // <InputNumber name="answerMinTime" label="答题最短时间(秒)" defaultValue={30} precision={0} />,
        // <InputNumber name="answerMaxTime" label="答题最长时间(秒)" defaultValue={60 * 2} precision={0} />,
        <Switch name="hasProbe" label="是否开启追问" defaultValue={true} disabled />
      ]}
    />
  );
});
export default JDFormInner;

// 文件: src/components/Common/Question/FormInner/index.js
export { default as ManualFormInner } from './ManualFormInner';
export { default as JDFormInner } from './JDFormInner';

// 文件: src/components/Common/Question/Detail/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useNavigate, useSearchParams } from 'react-router-dom';
import Fetch from '@kne/react-fetch';


AI智能面试题目生成系统 V1.0                                                    第 25 页
--------------------------------------------------------------------------------
import { Button, Flex, Typography } from 'antd';
import Digital from './Digital';
import Task from './Task';
import Actions from '../Actions';
import { useRef, useState } from 'react';
import Probe from './Probe';
const tabComponentMapping = {
  digital: Digital,
  task: Task,
  probe: Probe
};
const Detail = createWithRemoteLoader({
  modules: ['components-core:Layout@StateBarPage', 'components-core:Layout@PageHeader', 'components-core:InfoPage@SplitLine', 'components-core:StateTag@StateTagEnum']
})(({ remoteModules, baseUrl, isClient, api }) => {
  const [StateBarPage, PageHeader, SplitLine, StateTagEnum] = remoteModules;
  const [searchParams, setSearchParams] = useSearchParams();
  const navigate = useNavigate();
  const ref = useRef();
  const [expanded, setExpanded] = useState(false);
  const id = searchParams.get('id');
  const activeKey = searchParams.get('tab') || 'digital';
  const TabComponent = tabComponentMapping[activeKey] || Digital;
  return (
    <Fetch
      {...Object.assign({}, api, {
        params: { id }
      })}
      render={({ data, reload }) => {
        return (
          <StateBarPage
            stateBar={{
              activeKey,
              onChange: key => {
                searchParams.set('tab', key);
                setSearchParams(searchParams.toString());
              },
              stateOption: [
                { tab: '数字人', key: 'digital' },
                { tab: '任务', key: 'task', hidden: isClient },
                {
                  tab: '追问问题',
                  key: 'probe',
                  disabled: !data.hasProbe
                }
              ].filter(({ hidden }) => !hidden)
            }}
            headerFixed={false}
            header={
              <Actions
                id={data.id}


AI智能面试题目生成系统 V1.0                                                    第 26 页
--------------------------------------------------------------------------------
                data={data}
                baseUrl={baseUrl}
                isClient={isClient}
                onSuccess={async () => {
                  await reload();
                  ref.current && ref.current.reload();
                }}
              >
                {actionProps => {
                  return (
                    <PageHeader buttonOptions={actionProps} info={`ID: ${data.id}`} title={data.title} iconType="icon-color-bangzhuyindao">
                      <div>{data.determinant}</div>
                      <Flex vertical gap={8}>
                        <SplitLine
                          dataSource={data}
                          columns={[
                            {
                              name: 'language',
                              title: '语言',
                              render: value => <StateTagEnum moduleName="language" name={value} />
                            },
                            {
                              name: 'preparationTime',
                              title: '准备时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'answerMinTime',
                              title: '答题最短时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'answerMaxTime',
                              title: '答题最长时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'hasProbe',
                              title: '是否开启追问',
                              valueIsEmpty: () => false,
                              render: value => (value ? '是' : '否')
                            },
                            {
                              name: 'parentId',
                              render: () => {
                                return (
                                  <Button
                                    type="link"
                                    className="btn-no-padding"
                                    onClick={() => {


AI智能面试题目生成系统 V1.0                                                    第 27 页
--------------------------------------------------------------------------------
                                      navigate(`${baseUrl}/question/detail?id=${data.parentId}`);
                                    }}
                                  >
                                    返回主问题
                                  </Button>
                                );
                              }
                            }
                          ]}
                        />
                      </Flex>
                      {data.jobDescription ? (
                        <Flex gap={4} align="baseline">
                          <div style={{ color: 'var(--font-color-grey-1)' }}>JD：</div>
                          <Typography.Paragraph
                            ellipsis={{
                              rows: 2,
                              expandable: 'collapsible',
                              expanded,
                              onExpand: (_, info) => setExpanded(info.expanded)
                            }}
                            title={data.jobDescription}
                          >
                            {data.jobDescription}
                          </Typography.Paragraph>
                        </Flex>
                      ) : null}
                    </PageHeader>
                  );
                }}
              </Actions>
            }
          >
            <TabComponent
              id={data.id}
              getCurrentRef={api => {
                ref.current = api;
              }}
              baseUrl={baseUrl}
              isClient={isClient}
            />
          </StateBarPage>
        );
      }}
    />
  );
});
export default Detail;

// 文件: src/components/Common/Question/Detail/Task/index.js


AI智能面试题目生成系统 V1.0                                                    第 28 页
--------------------------------------------------------------------------------
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useRef, useEffect } from 'react';
import CompleteDigitalTask from '../../../Task/Actions/CompleteDigitalTask';
const Task = createWithRemoteLoader({
  modules: ['components-core:Table@TablePage', 'components-core:Global@usePreset', 'components-admin:Task@getColumns', 'components-admin:Task@Actions']
})(({ remoteModules, id, getCurrentRef }) => {
  const [TablePage, usePreset, getColumns, Actions] = remoteModules;
  const { apis } = usePreset();
  const ref = useRef();
  useEffect(() => {
    getCurrentRef(ref.current);
  });
  return (
    <TablePage
      {...Object.assign({}, apis.task.list, {
        params: {
          filter: {
            targetId: id
          }
        }
      })}
      ref={ref}
      pagination={{ paramsType: 'params' }}
      columns={[
        ...getColumns(),
        {
          name: 'options',
          title: '操作',
          type: 'options',
          fixed: 'right',
          valueOf: item => {
            return {
              children: ({ more, buttonProps }) => (
                <Actions
                  getManualTaskAction={data => {
                    if (data.runnerType === 'manual' && data.status === 'pending' && data.type === 'digital') {
                      return CompleteDigitalTask;
                    }
                  }}
                  buttonProps={buttonProps}
                  data={item}
                  more={more}
                  onSuccess={() => {
                    ref.current.reload();
                  }}
                />
              )
            };
          }
        }


AI智能面试题目生成系统 V1.0                                                    第 29 页
--------------------------------------------------------------------------------
      ]}
    />
  );
});
export default Task;

// 文件: src/components/Common/Question/Detail/Digital/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import getColumns from './getColumns';
import { useEffect, useRef } from 'react';
const Digital = createWithRemoteLoader({
  modules: ['components-core:Table@TablePage', 'components-core:Global@usePreset', 'components-core:File@FileLink', 'components-core:Image', 'components-core:StateTag']
})(({ remoteModules, id, isClient, getCurrentRef }) => {
  const [TablePage, usePreset, FileLink, Image, StateTag] = remoteModules;
  const { apis } = usePreset();
  const ref = useRef();
  useEffect(() => {
    getCurrentRef(ref.current);
  });
  return (
    <TablePage
      {...Object.assign({}, isClient ? apis.clientAdmin.question.digitalList : apis.question.digitalList, {
        params: { id }
      })}
      ref={ref}
      columns={[
        ...getColumns({
          renderFilePreview: ({ id }) => {
            return id ? <FileLink id={id}>查看</FileLink> : <StateTag text="暂无数据" type="other" />;
          },
          renderAvatar: id => {
            return <Image.Avatar id={id} size={40} shape="square" />;
          }
        })
      ]}
    />
  );
});
export default Digital;

// 文件: src/components/Common/Question/Detail/Digital/getColumns.js
import get from 'lodash/get';
const getColumns = ({ renderFilePreview, renderAvatar }) => {
  return [
    {
      name: 'id',
      title: 'ID',
      type: 'serialNumber'
    },
    {


AI智能面试题目生成系统 V1.0                                                    第 30 页
--------------------------------------------------------------------------------
      name: 'name',
      title: '名称',
      valueOf: item => {
        return item.digital.name;
      }
    },
    {
      name: 'cover',
      title: '封面',
      valueOf: item => {
        const cover = get(item, 'digital.cover');
        return cover && renderAvatar(cover);
      }
    },
    {
      name: 'video',
      title: '视频',
      valueOf: item => {
        return renderFilePreview({ id: item.video });
      }
    },
    {
      name: 'text',
      title: '视频文本',
      type: 'description'
    },
    {
      name: 'waitingVideo',
      title: '等待视频',
      valueOf: item => {
        const waitingVideo = get(item, 'digital.waitingVideo');
        return waitingVideo && renderFilePreview({ id: waitingVideo });
      }
    },
    {
      name: 'background',
      title: '背景',
      valueOf: item => {
        const background = get(item, 'digital.background');
        return background && renderFilePreview({ id: background });
      }
    }
  ];
};
export default getColumns;

// 文件: src/components/Common/Question/Detail/Probe/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import getColumns from '../../List/getColumns';
import Actions from '../../Actions';



================================================================================

## 后30页


AI智能面试题目生成系统 V1.0                                                    第 31 页
--------------------------------------------------------------------------------
              type: 'boolean',
              description: '是否包含追问问题'
            },
            options: {
              type: 'object',
              description: '扩展字段'
            },
            jobDescription: {
              type: 'string',
              description: 'JD描述'
            },
            questionCount: {
              type: 'number',
              description: '默认生成数量'
            }
          }
        }
      }
    },
    async request => {
      return services.question.jdBatchCreate(request.userInfo, request.body);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/list`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题列表',
        query: {
          type: 'object',
          properties: {
            filter: {
              type: 'object'
            },
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            }
          }
        }
      }
    },
    async request => {
      return services.question.list(request.userInfo, request.query);
    }


AI智能面试题目生成系统 V1.0                                                    第 32 页
--------------------------------------------------------------------------------
  );
  fastify.get(
    `${options.prefix}/admin/question/list-digital`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题数字人列表',
        query: {
          type: 'object',
          properties: {
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            },
            digitalId: {
              type: 'string'
            },
            filter: {
              type: 'object'
            }
          },
          required: ['digitalId']
        }
      }
    },
    async request => {
      return services.question.listByDigital(request.userInfo, request.query);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/digital-list`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取题目数字人列表',
        query: {
          type: 'object',
          properties: {
            perPage: {
              type: 'number',
              default: 20
            },
            currentPage: {
              type: 'number',
              default: 1
            },


AI智能面试题目生成系统 V1.0                                                    第 33 页
--------------------------------------------------------------------------------
            id: {
              type: 'string'
            }
          },
          required: ['id']
        }
      }
    },
    async request => {
      return services.question.digitalList(request.userInfo, request.query);
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/detail`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '获取问题详情',
        query: {
          type: 'object',
          properties: {
            id: {
              type: 'string'
            }
          },
          required: ['id']
        }
      }
    },
    async request => {
      return services.question.detail(request.userInfo, request.query);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/save`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '保存问题',
        body: merge({}, questionSchema, {
          required: ['id'],
          properties: {
            id: {
              type: 'string'
            }
          }
        })
      }
    },
    async request => {


AI智能面试题目生成系统 V1.0                                                    第 34 页
--------------------------------------------------------------------------------
      return services.question.save(request.userInfo, request.body);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/remove`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '删除问题',
        body: {
          type: 'object',
          properties: {
            id: {
              type: 'string'
            }
          }
        }
      }
    },
    async request => {
      await services.question.remove(request.userInfo, request.body);
      return {};
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/generate-digital`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '生成问题数字人',
        body: {
          type: 'object',
          properties: {
            digitalId: {
              type: 'string'
            },
            questionIds: {
              type: 'array',
              items: {
                type: 'string'
              },
              minItems: 1
            },
            generateProbeDigital: {
              type: 'boolean'
            },
            reGenerateDigital: {
              type: 'boolean'
            }
          },


AI智能面试题目生成系统 V1.0                                                    第 35 页
--------------------------------------------------------------------------------
          required: ['digitalId', 'questionIds']
        }
      }
    },
    async request => {
      return await services.question.generateDigital(request.userInfo, request.body);
    }
  );
  fastify.post(
    `${options.prefix}/admin/question/generate-probe`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '生成追问问题',
        body: {
          type: 'object',
          properties: {
            questionIds: {
              type: 'array',
              items: {
                type: 'string'
              },
              minItems: 1
            },
            probeQuestionsNumber: {
              type: 'number'
            }
          },
          required: ['questionIds']
        }
      }
    },
    async request => {
      await services.question.generateProbe(request.userInfo, request.body);
      return {};
    }
  );
  fastify.get(
    `${options.prefix}/admin/question/video-generation-status/:taskId`,
    {
      onRequest: [authenticate.user, authenticate.admin],
      schema: {
        summary: '查询视频生成任务状态',
        params: {
          type: 'object',
          properties: {
            taskId: {
              type: 'string'
            }
          },


AI智能面试题目生成系统 V1.0                                                    第 36 页
--------------------------------------------------------------------------------
          required: ['taskId']
        }
      }
    },
    async request => {
      return services.question.getVideoGenerationStatus(request.userInfo, request.params);
    }
  );
});

// 文件: server/libs/models/question.js
module.exports = ({ DataTypes, definePrimaryType, options }) => {
  return {
    model: {
      title: {
        type: DataTypes.STRING,
        allowNull: false,
        comment: '题目'
      },
      type: {
        type: DataTypes.ENUM('main', 'probe'),
        defaultValue: 'main',
        comment: '题目类型: main: 主要问题, probe: 追问问题'
      },
      language: {
        type: DataTypes.ENUM('zh-CN', 'en-US'),
        defaultValue: 'zh-CN',
        comment: '语言'
      },
      preparationTime: {
        type: DataTypes.INTEGER,
        defaultValue: 30,
        comment: '准备时间,单位秒'
      },
      answerType: {
        type: DataTypes.ENUM('single', 'multiple', 'text', 'video'),
        defaultValue: 'video',
        comment: '答案类型: single: 单选, multiple: 多选, text: 文本, video: 视频'
      },
      answerMaxTime: {
        type: DataTypes.INTEGER,
        defaultValue: 60 * 2,
        comment: '答题最长时间,单位秒'
      },
      answerMinTime: {
        type: DataTypes.INTEGER,
        defaultValue: 30,
        comment: '答题最短时间,单位秒'
      },
      determinant: {


AI智能面试题目生成系统 V1.0                                                    第 37 页
--------------------------------------------------------------------------------
        type: DataTypes.TEXT,
        comment: '判定规则(主要提供给模型)'
      },
      parentId: definePrimaryType('parentId', {
        comment: '追问问题关联的主问题ID'
      }),
      groups: {
        type: DataTypes.JSON,
        comment: '分组列表',
        defaultValue: []
      },
      hasProbe: {
        type: DataTypes.BOOLEAN,
        comment: '是否包含追问问题',
        defaultValue: false
      },
      options: {
        type: DataTypes.JSON,
        comment: '扩展字段'
      },
      jobDescription: {
        type: DataTypes.TEXT,
        comment: '职位描述'
      }
    },
    associate: ({ question, client }) => {
      question.belongsTo(options.getUserModel(), {
        comment: '创建人',
        allowNull: false
      });
      question.belongsTo(client, {
        comment: '所属客户',
        allowNull: true
      });
    },
    options: {
      comment: '问题',
      indexes: [
        {
          fields: ['title', 'parent_id', 'deleted_at'],
          unique: true
        }
      ]
    }
  };
};

// 文件: src/components/Admin/Question/index.js
import { Routes, Route } from 'react-router-dom';
import { createWithRemoteLoader } from '@kne/remote-loader';


AI智能面试题目生成系统 V1.0                                                    第 38 页
--------------------------------------------------------------------------------
const Question = createWithRemoteLoader({
  modules: ['ai-interview-v2:Common', 'components-core:Global@usePreset']
})(({ baseUrl, remoteModules }) => {
  const [Common, usePreset] = remoteModules;
  const { List, Detail } = Common.Question;
  const { apis } = usePreset();
  return (
    <Routes>
      <Route index element={<List baseUrl={baseUrl} api={apis.question.list} />} />
      <Route path="detail" element={<Detail baseUrl={baseUrl} api={apis.question.detail} />} />
    </Routes>
  );
});
export default Question;

// 文件: src/components/Common/Question/index.js
import List, { getColumns } from './List';
import { ManualFormInner, JDFormInner } from './FormInner';
import Detail from './Detail';
import Actions from './Actions';
export default {
  List,
  getColumns,
  ManualFormInner,
  JDFormInner,
  Detail,
  Actions
};

// 文件: src/components/Common/Question/VideoGenerationStatus/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useEffect, useState } from 'react';
import { Progress, Typography, Alert, Space } from 'antd';
const VideoGenerationStatus = createWithRemoteLoader({
  modules: ['components-core:Global@usePreset']
})(({ remoteModules, taskId, onComplete, isClient }) => {
  const [usePreset] = remoteModules;
  const { apis, ajax } = usePreset();
  const [status, setStatus] = useState(null);
  const [loading, setLoading] = useState(true);
  const fetchStatus = async () => {
    try {
      const { data: resData } = await ajax(
        Object.assign({}, isClient ? apis.clientAdmin.question.getVideoGenerationStatus : apis.question.getVideoGenerationStatus, {
          urlParams: { taskId }
        })
      );
      if (resData.code === 0) {
        setStatus(resData.data);
        // 如果任务完成，调用回调函数


AI智能面试题目生成系统 V1.0                                                    第 39 页
--------------------------------------------------------------------------------
        if (resData.data.status === 'success' || resData.data.status === 'failed') {
          onComplete && onComplete(resData.data);
        }
      }
    } catch (error) {
      console.error('获取任务状态失败:', error);
    } finally {
      setLoading(false);
    }
  };
  useEffect(() => {
    if (!taskId) return;
    // 立即获取一次状态
    fetchStatus();
    // 如果任务还在进行中，每3秒轮询一次
    const interval = setInterval(() => {
      if (status && (status.status === 'pending' || status.status === 'running')) {
        fetchStatus();
      } else {
        clearInterval(interval);
      }
    }, 3000);
    return () => clearInterval(interval);
  }, [taskId, status?.status]);
  if (loading) {
    return <div>加载中...</div>;
  }
  if (!status) {
    return <Alert message="无法获取任务状态" type="error" />;
  }
  const getStatusColor = status => {
    switch (status) {
      case 'success':
        return 'success';
      case 'failed':
        return 'exception';
      case 'running':
        return 'active';
      default:
        return 'normal';
    }
  };
  const getStatusText = status => {
    switch (status) {
      case 'pending':
        return '等待处理';
      case 'running':
        return '生成中';
      case 'success':
        return '生成成功';


AI智能面试题目生成系统 V1.0                                                    第 40 页
--------------------------------------------------------------------------------
      case 'failed':
        return '生成失败';
      case 'canceled':
        return '已取消';
      default:
        return '未知状态';
    }
  };
  return (
    <Space direction="vertical" style={{ width: '100%' }}>
      <Typography.Text strong>视频生成状态</Typography.Text>
      <Progress percent={status.progress || 0} status={getStatusColor(status.status)} format={() => `${status.progress || 0}%`} />
      <Typography.Text>状态: {getStatusText(status.status)}</Typography.Text>
      {status.msg && <Alert message={status.msg} type={status.status === 'failed' ? 'error' : 'info'} showIcon />}
      {status.status === 'success' && status.options?.chanjingCallback?.video_url && (
        <Alert
          message="视频生成成功！"
          description={
            <div>
              <p>视频已保存到数据库</p>
              <a href={status.options.chanjingCallback.video_url} target="_blank" rel="noopener noreferrer">
                查看生成的视频
              </a>
            </div>
          }
          type="success"
          showIcon
        />
      )}
    </Space>
  );
});
export default VideoGenerationStatus;

// 文件: src/components/Common/Question/FormInner/ManualFormInner.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const ManualFormInner = createWithRemoteLoader({
  modules: ['components-core:FormInfo']
})(({ remoteModules, isProbe, isEdit }) => {
  const [FormInfo] = remoteModules;
  const { TextArea, Select, InputNumber, Switch } = FormInfo.fields;
  return (
    <FormInfo
      list={[
        <TextArea name="title" label="题目" rule="REQ LEN-0-500" disabled={isEdit} />,
        <Select
          disabled={isEdit}
          name="language"
          label="语言"
          rule="REQ"


AI智能面试题目生成系统 V1.0                                                    第 41 页
--------------------------------------------------------------------------------
          options={[
            { label: '中文', value: 'zh-CN' },
            { label: '英文', value: 'en-US' }
          ]}
          defaultValue="zh-CN"
        />,
        <InputNumber name="preparationTime" label="准备时间(秒)" defaultValue={30} precision={0} />,
        <InputNumber name="answerMinTime" label="答题最短时间(秒)" defaultValue={30} precision={0} />,
        <InputNumber name="answerMaxTime" label="答题最长时间(秒)" defaultValue={60 * 2} precision={0} />,
        <Switch name="hasProbe" label="是否开启追问" display={!isProbe} />
      ]}
    />
  );
});
export default ManualFormInner;

// 文件: src/components/Common/Question/FormInner/JDFormInner.js
import { createWithRemoteLoader } from '@kne/remote-loader';
const JDFormInner = createWithRemoteLoader({
  modules: ['components-core:FormInfo']
})(({ remoteModules }) => {
  const [FormInfo] = remoteModules;
  const { TextArea, Select, InputNumber, Switch } = FormInfo.fields;
  return (
    <FormInfo
      column={1}
      list={[
        <TextArea name="jobDescription" label="JD" rule="REQ LEN-0-5000" />,
        <Select
          name="language"
          label="语言"
          rule="REQ"
          options={[
            { label: '中文', value: 'zh-CN' },
            { label: '英文', value: 'en-US' }
          ]}
          defaultValue="zh-CN"
        />,
        <InputNumber name="questionCount" label="生成数量" defaultValue={5} min={1} max={10} rule="REQ" precision={0} />,
        // <InputNumber name="preparationTime" label="准备时间(秒)" defaultValue={30} disabled precision={0} />,
        // <InputNumber name="answerMinTime" label="答题最短时间(秒)" defaultValue={30} precision={0} />,
        // <InputNumber name="answerMaxTime" label="答题最长时间(秒)" defaultValue={60 * 2} precision={0} />,
        <Switch name="hasProbe" label="是否开启追问" defaultValue={true} disabled />
      ]}
    />
  );
});
export default JDFormInner;

// 文件: src/components/Common/Question/FormInner/index.js


AI智能面试题目生成系统 V1.0                                                    第 42 页
--------------------------------------------------------------------------------
export { default as ManualFormInner } from './ManualFormInner';
export { default as JDFormInner } from './JDFormInner';

// 文件: src/components/Common/Question/Detail/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useNavigate, useSearchParams } from 'react-router-dom';
import Fetch from '@kne/react-fetch';
import { Button, Flex, Typography } from 'antd';
import Digital from './Digital';
import Task from './Task';
import Actions from '../Actions';
import { useRef, useState } from 'react';
import Probe from './Probe';
const tabComponentMapping = {
  digital: Digital,
  task: Task,
  probe: Probe
};
const Detail = createWithRemoteLoader({
  modules: ['components-core:Layout@StateBarPage', 'components-core:Layout@PageHeader', 'components-core:InfoPage@SplitLine', 'components-core:StateTag@StateTagEnum']
})(({ remoteModules, baseUrl, isClient, api }) => {
  const [StateBarPage, PageHeader, SplitLine, StateTagEnum] = remoteModules;
  const [searchParams, setSearchParams] = useSearchParams();
  const navigate = useNavigate();
  const ref = useRef();
  const [expanded, setExpanded] = useState(false);
  const id = searchParams.get('id');
  const activeKey = searchParams.get('tab') || 'digital';
  const TabComponent = tabComponentMapping[activeKey] || Digital;
  return (
    <Fetch
      {...Object.assign({}, api, {
        params: { id }
      })}
      render={({ data, reload }) => {
        return (
          <StateBarPage
            stateBar={{
              activeKey,
              onChange: key => {
                searchParams.set('tab', key);
                setSearchParams(searchParams.toString());
              },
              stateOption: [
                { tab: '数字人', key: 'digital' },
                { tab: '任务', key: 'task', hidden: isClient },
                {
                  tab: '追问问题',
                  key: 'probe',
                  disabled: !data.hasProbe


AI智能面试题目生成系统 V1.0                                                    第 43 页
--------------------------------------------------------------------------------
                }
              ].filter(({ hidden }) => !hidden)
            }}
            headerFixed={false}
            header={
              <Actions
                id={data.id}
                data={data}
                baseUrl={baseUrl}
                isClient={isClient}
                onSuccess={async () => {
                  await reload();
                  ref.current && ref.current.reload();
                }}
              >
                {actionProps => {
                  return (
                    <PageHeader buttonOptions={actionProps} info={`ID: ${data.id}`} title={data.title} iconType="icon-color-bangzhuyindao">
                      <div>{data.determinant}</div>
                      <Flex vertical gap={8}>
                        <SplitLine
                          dataSource={data}
                          columns={[
                            {
                              name: 'language',
                              title: '语言',
                              render: value => <StateTagEnum moduleName="language" name={value} />
                            },
                            {
                              name: 'preparationTime',
                              title: '准备时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'answerMinTime',
                              title: '答题最短时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'answerMaxTime',
                              title: '答题最长时间',
                              render: value => `${value}s`
                            },
                            {
                              name: 'hasProbe',
                              title: '是否开启追问',
                              valueIsEmpty: () => false,
                              render: value => (value ? '是' : '否')
                            },
                            {


AI智能面试题目生成系统 V1.0                                                    第 44 页
--------------------------------------------------------------------------------
                              name: 'parentId',
                              render: () => {
                                return (
                                  <Button
                                    type="link"
                                    className="btn-no-padding"
                                    onClick={() => {
                                      navigate(`${baseUrl}/question/detail?id=${data.parentId}`);
                                    }}
                                  >
                                    返回主问题
                                  </Button>
                                );
                              }
                            }
                          ]}
                        />
                      </Flex>
                      {data.jobDescription ? (
                        <Flex gap={4} align="baseline">
                          <div style={{ color: 'var(--font-color-grey-1)' }}>JD：</div>
                          <Typography.Paragraph
                            ellipsis={{
                              rows: 2,
                              expandable: 'collapsible',
                              expanded,
                              onExpand: (_, info) => setExpanded(info.expanded)
                            }}
                            title={data.jobDescription}
                          >
                            {data.jobDescription}
                          </Typography.Paragraph>
                        </Flex>
                      ) : null}
                    </PageHeader>
                  );
                }}
              </Actions>
            }
          >
            <TabComponent
              id={data.id}
              getCurrentRef={api => {
                ref.current = api;
              }}
              baseUrl={baseUrl}
              isClient={isClient}
            />
          </StateBarPage>
        );


AI智能面试题目生成系统 V1.0                                                    第 45 页
--------------------------------------------------------------------------------
      }}
    />
  );
});
export default Detail;

// 文件: src/components/Common/Question/Detail/Task/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useRef, useEffect } from 'react';
import CompleteDigitalTask from '../../../Task/Actions/CompleteDigitalTask';
const Task = createWithRemoteLoader({
  modules: ['components-core:Table@TablePage', 'components-core:Global@usePreset', 'components-admin:Task@getColumns', 'components-admin:Task@Actions']
})(({ remoteModules, id, getCurrentRef }) => {
  const [TablePage, usePreset, getColumns, Actions] = remoteModules;
  const { apis } = usePreset();
  const ref = useRef();
  useEffect(() => {
    getCurrentRef(ref.current);
  });
  return (
    <TablePage
      {...Object.assign({}, apis.task.list, {
        params: {
          filter: {
            targetId: id
          }
        }
      })}
      ref={ref}
      pagination={{ paramsType: 'params' }}
      columns={[
        ...getColumns(),
        {
          name: 'options',
          title: '操作',
          type: 'options',
          fixed: 'right',
          valueOf: item => {
            return {
              children: ({ more, buttonProps }) => (
                <Actions
                  getManualTaskAction={data => {
                    if (data.runnerType === 'manual' && data.status === 'pending' && data.type === 'digital') {
                      return CompleteDigitalTask;
                    }
                  }}
                  buttonProps={buttonProps}
                  data={item}
                  more={more}
                  onSuccess={() => {


AI智能面试题目生成系统 V1.0                                                    第 46 页
--------------------------------------------------------------------------------
                    ref.current.reload();
                  }}
                />
              )
            };
          }
        }
      ]}
    />
  );
});
export default Task;

// 文件: src/components/Common/Question/Detail/Digital/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import getColumns from './getColumns';
import { useEffect, useRef } from 'react';
const Digital = createWithRemoteLoader({
  modules: ['components-core:Table@TablePage', 'components-core:Global@usePreset', 'components-core:File@FileLink', 'components-core:Image', 'components-core:StateTag']
})(({ remoteModules, id, isClient, getCurrentRef }) => {
  const [TablePage, usePreset, FileLink, Image, StateTag] = remoteModules;
  const { apis } = usePreset();
  const ref = useRef();
  useEffect(() => {
    getCurrentRef(ref.current);
  });
  return (
    <TablePage
      {...Object.assign({}, isClient ? apis.clientAdmin.question.digitalList : apis.question.digitalList, {
        params: { id }
      })}
      ref={ref}
      columns={[
        ...getColumns({
          renderFilePreview: ({ id }) => {
            return id ? <FileLink id={id}>查看</FileLink> : <StateTag text="暂无数据" type="other" />;
          },
          renderAvatar: id => {
            return <Image.Avatar id={id} size={40} shape="square" />;
          }
        })
      ]}
    />
  );
});
export default Digital;

// 文件: src/components/Common/Question/Detail/Digital/getColumns.js
import get from 'lodash/get';
const getColumns = ({ renderFilePreview, renderAvatar }) => {


AI智能面试题目生成系统 V1.0                                                    第 47 页
--------------------------------------------------------------------------------
  return [
    {
      name: 'id',
      title: 'ID',
      type: 'serialNumber'
    },
    {
      name: 'name',
      title: '名称',
      valueOf: item => {
        return item.digital.name;
      }
    },
    {
      name: 'cover',
      title: '封面',
      valueOf: item => {
        const cover = get(item, 'digital.cover');
        return cover && renderAvatar(cover);
      }
    },
    {
      name: 'video',
      title: '视频',
      valueOf: item => {
        return renderFilePreview({ id: item.video });
      }
    },
    {
      name: 'text',
      title: '视频文本',
      type: 'description'
    },
    {
      name: 'waitingVideo',
      title: '等待视频',
      valueOf: item => {
        const waitingVideo = get(item, 'digital.waitingVideo');
        return waitingVideo && renderFilePreview({ id: waitingVideo });
      }
    },
    {
      name: 'background',
      title: '背景',
      valueOf: item => {
        const background = get(item, 'digital.background');
        return background && renderFilePreview({ id: background });
      }
    }
  ];


AI智能面试题目生成系统 V1.0                                                    第 48 页
--------------------------------------------------------------------------------
};
export default getColumns;

// 文件: src/components/Common/Question/Detail/Probe/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import getColumns from '../../List/getColumns';
import Actions from '../../Actions';
import { useRef, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
const Probe = createWithRemoteLoader({
  modules: ['components-core:Table@TablePage', 'components-core:Global@usePreset']
})(({ remoteModules, id, getCurrentRef, baseUrl, isClient }) => {
  const [TablePage, usePreset] = remoteModules;
  const { apis, setting } = usePreset();
  const ref = useRef();
  const navigate = useNavigate();
  useEffect(() => {
    getCurrentRef(ref.current);
  });
  return (
    <TablePage
      {...Object.assign({}, isClient ? apis.clientAdmin.question.list : apis.question.list, {
        params: {
          filter: {
            parentId: id,
            type: 'probe'
          }
        }
      })}
      ref={ref}
      pagination={{ paramsType: 'params' }}
      columns={[
        ...getColumns({ navigate, baseUrl, systemType: setting.systemType, isClient }),
        {
          name: 'options',
          title: '操作',
          type: 'options',
          fixed: 'right',
          valueOf: item => {
            return {
              children: ({ more, buttonProps }) => (
                <Actions
                  buttonProps={buttonProps}
                  id={item.id}
                  data={item}
                  more={more}
                  type="link"
                  className="btn-no-padding"
                  onSuccess={() => {
                    ref.current?.reload();


AI智能面试题目生成系统 V1.0                                                    第 49 页
--------------------------------------------------------------------------------
                  }}
                />
              )
            };
          }
        }
      ]}
    />
  );
});
export default Probe;

// 文件: src/components/Common/Question/List/index.js
import List from './List';
import getColumns from './getColumns';
export default List;
export { getColumns };

// 文件: src/components/Common/Question/List/List.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { useRef, useState } from 'react';
import { App, Button, Space } from 'antd';
import { useNavigate } from 'react-router-dom';
import { ManualFormInner, JDFormInner } from '../FormInner';
import getColumns from './getColumns';
import Actions from '../Actions';
import AddDigitalTask from '../Actions/AddDigitalTask';
const List = createWithRemoteLoader({
  modules: ['components-core:Layout@TablePage', 'components-core:Global@usePreset', 'components-core:Filter', 'components-core:FormInfo@useFormModal', 'components-core:ButtonGroup', 'components-core:Tooltip', 'components-core:Enum']
})(({ remoteModules, baseUrl, api, isClient }) => {
  const [TablePage, usePreset, Filter, useFormModal, ButtonGroup, Tooltip, Enum] = remoteModules;
  const { SearchInput, getFilterValue, fields: filterFields } = Filter;
  const { InputFilterItem, AdvancedSelectFilterItem } = filterFields;
  const ref = useRef(null);
  const [filter, setFilter] = useState([]);
  const [selected, setSelected] = useState({
    selectedRowKeys: [],
    selectedRows: []
  });
  const { apis, ajax, enums, setting } = usePreset();
  const filterValue = getFilterValue(filter);
  const formModal = useFormModal();
  const { message } = App.useApp();
  const navigate = useNavigate();
  const onSelectChange = (selectedRowKeys, selectedRows) => {
    setSelected({ selectedRowKeys, selectedRows });
  };
  return (
    <TablePage
      {...api}


AI智能面试题目生成系统 V1.0                                                    第 50 页
--------------------------------------------------------------------------------
      params={{ filter: filterValue }}
      ref={ref}
      pagination={{ paramsType: 'params' }}
      rowSelection={{
        type: 'checkbox',
        hideSelectAll: true,
        selectedRowKeys: selected.selectedRowKeys,
        onChange: onSelectChange,
        getCheckboxProps: record => {
          return {
            disabled: selected.selectedRows?.length ? record.language !== selected.selectedRows[0].language : false // Column configuration not to be checked
          };
        }
      }}
      name="admin-question-list"
      topArea={
        <>
          已选：{selected.selectedRowKeys?.length}
          <Tooltip
            placement="topLeft"
            content={
              selected.selectedRows?.length ? (
                <>
                  只能选择语言为【
                  <Enum moduleName={'language'} name={selected.selectedRows[0].language} />
                  】的题目
                </>
              ) : (
                '请选择要批量添加数字人任务的题目，只能选择相同语言的题目'
              )
            }
          >
            <AddDigitalTask
              size="small"
              type="link"
              data={{ language: selected.selectedRows?.[0]?.language, hasProbe: true }}
              disabled={!selected.selectedRows?.length}
              questionIds={selected.selectedRowKeys}
              isClient={isClient}
              onSuccess={() => {
                setSelected({
                  selectedRowKeys: [],
                  selectedRows: []
                });
                ref.current?.reload?.();
              }}
            >
              批量添加数字人任务
            </AddDigitalTask>
          </Tooltip>


AI智能面试题目生成系统 V1.0                                                    第 51 页
--------------------------------------------------------------------------------
        </>
      }
      page={{
        filter: {
          value: filter,
          onChange: setFilter,
          list: [
            [
              <InputFilterItem label="题目ID" name="id" />,
              <AdvancedSelectFilterItem
                label="语言"
                name="language"
                single
                api={{
                  loader: () => {
                    return {
                      pageData: enums.language().map(item => ({ value: item.value, label: item.description }))
                    };
                  }
                }}
              />,
              <AdvancedSelectFilterItem
                label="是否开启追问"
                name="hasProbe"
                single
                api={{
                  loader: () => {
                    return {
                      pageData: [
                        { label: '是', value: true },
                        { label: '否', value: false }
                      ]
                    };
                  }
                }}
              />
            ]
          ]
        },
        titleExtra: (
          <Space gap={8} align="center">
            <SearchInput name="title" label="题目" />
            <div style={{ maxWidth: '100px' }}>
              <ButtonGroup
                compact
                more={<Button type="primary">添加</Button>}
                list={[
                  {
                    children: '手动添加',
                    onClick: () => {


AI智能面试题目生成系统 V1.0                                                    第 52 页
--------------------------------------------------------------------------------
                      formModal({
                        title: '添加题目',
                        autoClose: true,
                        formProps: {
                          onSubmit: async formData => {
                            const { data: resData } = await ajax(
                              Object.assign({}, isClient ? apis.clientAdmin.question.create : apis.question.create, {
                                data: Object.assign({}, formData)
                              })
                            );
                            if (resData.code !== 0) {
                              return false;
                            }
                            message.success('添加成功');
                            ref.current.reload();
                          }
                        },
                        children: <ManualFormInner />
                      });
                    }
                  },
                  {
                    children: '根据JD添加',
                    onClick: () => {
                      formModal({
                        size: 'small',
                        title: '添加题目',
                        autoClose: true,
                        formProps: {
                          onSubmit: async formData => {
                            const { data: resData } = await ajax(
                              Object.assign({}, isClient ? apis.clientAdmin.question.jdBatchCreate : apis.question.jdBatchCreate, {
                                data: Object.assign({}, formData)
                              })
                            );
                            if (resData.code !== 0) {
                              return false;
                            }
                            message.success('添加成功');
                            ref.current.reload();
                          }
                        },
                        children: <JDFormInner />
                      });
                    }
                  }
                ]}
              />
            </div>
          </Space>


AI智能面试题目生成系统 V1.0                                                    第 53 页
--------------------------------------------------------------------------------
        )
      }}
      columns={[
        ...getColumns({ navigate, baseUrl, systemType: setting.systemType, isClient }),
        {
          name: 'options',
          title: '操作',
          type: 'options',
          fixed: 'right',
          valueOf: item => {
            return {
              children: <Actions id={item.id} data={item} type="link" className="btn-no-padding" onSuccess={() => ref.current?.reload()} isClient={isClient} />
            };
          }
        }
      ]}
    />
  );
});
export default List;

// 文件: src/components/Common/Question/List/getColumns.js
import { Space, Spin } from 'antd';
import RemoteLoader from '@kne/remote-loader';
const getColumns = ({ navigate, baseUrl, systemType, isClient }) => {
  return [
    {
      name: 'id',
      title: 'ID',
      type: 'serialNumber',
      primary: true,
      hover: true,
      onClick: ({ colItem }) => {
        navigate(`${baseUrl}/question/detail?id=${colItem.id}`);
      }
    },
    {
      name: 'title',
      title: '题目',
      type: 'mainInfo',
      onClick: ({ colItem }) => {
        navigate(`${baseUrl}/question/detail?id=${colItem.id}`);
      }
    },
    {
      name: 'questionDigitals',
      title: '数字人',
      type: 'serialNumber',
      valueOf: item => {
        return (


AI智能面试题目生成系统 V1.0                                                    第 54 页
--------------------------------------------------------------------------------
          <Space size={0} split="," wrap>
            {item.questionDigitals.map(({ id, digital, video }) => {
              return (
                <RemoteLoader key={id} module="components-core:File@FileLink" id={video} title={digital.name}>
                  {digital.name}
                  {digital.video}
                </RemoteLoader>
              );
            })}
            {item.taskCount > 0 && <Spin>{item.taskCount}个数字人任务正在执行</Spin>}
          </Space>
        );
      }
    },
    {
      name: 'language',
      title: '语言',
      type: 'tag',
      valueOf: ({ language }) =>
        language && {
          isEnum: true,
          moduleName: 'language',
          name: language
        }
    },
    {
      name: 'client.name',
      title: '所属客户',
      type: 'mainInfo',
      hidden: systemType !== '1' || !!isClient,
      onClick: ({ colItem }) => {
        navigate(`${baseUrl}/client/detail?id=${colItem.clientId}`);
      }
    },
    {
      name: 'type',
      title: '主问题',
      type: 'serialNumberShort',
      valueOf: item => (item.type === 'main' ? '是' : '否')
    },
    {
      name: 'hasProbe',
      title: '是否开启追问',
      type: 'serialNumberShort',
      valueOf: item => (item.hasProbe ? '是' : '否')
    },
    {
      name: 'preparationTime',
      title: '准备时间(秒)',
      type: 'time'


AI智能面试题目生成系统 V1.0                                                    第 55 页
--------------------------------------------------------------------------------
    },
    {
      name: 'answerMinTime',
      title: '答题最短时间(秒)',
      type: 'time'
    },
    {
      name: 'answerMaxTime',
      title: '答题最长时间(秒)',
      type: 'time'
    }
  ];
};
export default getColumns;

// 文件: src/components/Common/Question/Actions/Remove.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { App } from 'antd';
const RemoveQuestion = createWithRemoteLoader({
  modules: ['components-core:Global@usePreset', 'components-core:ConfirmButton']
})(({ remoteModules, data, onSuccess, isClient, ...props }) => {
  const [usePreset, ConfirmButton] = remoteModules;
  const { ajax, apis } = usePreset();
  const { message } = App.useApp();
  return (
    <ConfirmButton
      {...props}
      onClick={async () => {
        const { data: resData } = await ajax(Object.assign({}, isClient ? apis.clientAdmin.question.remove : apis.question.remove, { data: { id: data.id } }));
        if (resData.code !== 0) {
          return;
        }
        message.success('删除成功');
        onSuccess && onSuccess();
      }}
    />
  );
});
export default RemoveQuestion;

// 文件: src/components/Common/Question/Actions/Edit.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { Button, App } from 'antd';
import { ManualFormInner } from '../FormInner';
const EditQuestion = createWithRemoteLoader({
  modules: ['components-core:FormInfo@useFormModal', 'components-core:Global@usePreset']
})(({ remoteModules, data, onSuccess, isClient, ...props }) => {
  const [useFormModal, usePreset] = remoteModules;
  const formModal = useFormModal();
  const { ajax, apis } = usePreset();


AI智能面试题目生成系统 V1.0                                                    第 56 页
--------------------------------------------------------------------------------
  const { message } = App.useApp();
  return (
    <Button
      {...props}
      onClick={() => {
        formModal({
          title: '编辑题目',
          autoClose: true,
          formProps: {
            data: Object.assign({}, data),
            onSubmit: async formData => {
              const { data: resData } = await ajax(
                Object.assign({}, isClient ? apis.clientAdmin.question.save : apis.question.save, {
                  data: Object.assign({}, formData, { id: data.id })
                })
              );
              if (resData.code !== 0) {
                return false;
              }
              message.success('保存成功');
              onSuccess?.();
            }
          },
          children: <ManualFormInner isProbe={!!data.parentId} isEdit />
        });
      }}
    />
  );
});
export default EditQuestion;

// 文件: src/components/Common/Question/Actions/AddProbe.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { Button, App } from 'antd';
import { ManualFormInner } from '../FormInner';
const AddProbe = createWithRemoteLoader({
  modules: ['components-core:FormInfo@useFormModal', 'components-core:Global@usePreset']
})(({ remoteModules, data, onSuccess, isClient, ...props }) => {
  const [useFormModal, usePreset] = remoteModules;
  const formModal = useFormModal();
  const { ajax, apis } = usePreset();
  const { message } = App.useApp();
  return (
    <Button
      {...props}
      onClick={() => {
        formModal({
          title: '添加追问问题',
          autoClose: true,
          formProps: {


AI智能面试题目生成系统 V1.0                                                    第 57 页
--------------------------------------------------------------------------------
            // data: Object.assign({}, data),
            onSubmit: async formData => {
              const { data: resData } = await ajax(
                Object.assign({}, isClient ? apis.clientAdmin.question.create : apis.question.create, {
                  data: Object.assign({}, formData, {
                    parentId: data.id,
                    type: 'probe'
                  })
                })
              );
              if (resData.code !== 0) {
                return false;
              }
              message.success('保存成功');
              onSuccess?.();
            }
          },
          children: <ManualFormInner isProbe />
        });
      }}
    />
  );
});
export default AddProbe;

// 文件: src/components/Common/Question/Actions/index.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import AddDigitalTask from './AddDigitalTask';
import EditQuestion from './Edit';
import RemoveQuestion from './Remove';
import { Button } from 'antd';
import { useNavigate } from 'react-router-dom';
import AddProbe from './AddProbe';
import AddProbeTask from './AddProbeTask';
const Actions = createWithRemoteLoader({
  modules: ['components-core:ButtonGroup', 'components-core:Icon']
})(({ remoteModules, children, baseUrl, ...props }) => {
  const [ButtonGroup, Icon] = remoteModules;
  const navigate = useNavigate();
  const actionList = [
    {
      type: 'primary',
      ...props,
      children: '编辑',
      buttonComponent: EditQuestion
    },
    {
      ...props,
      children: '添加数字人任务',
      buttonComponent: AddDigitalTask


AI智能面试题目生成系统 V1.0                                                    第 58 页
--------------------------------------------------------------------------------
    },
    {
      ...props,
      children: '添加追问问题任务',
      hidden: !props?.data.hasProbe,
      buttonComponent: AddProbeTask
    },
    {
      ...props,
      children: '添加追问问题',
      hidden: !props?.data.hasProbe,
      buttonComponent: AddProbe
    },
    {
      ...props,
      children: '删除',
      buttonComponent: RemoveQuestion,
      onSuccess: () => {
        if (baseUrl) {
          navigate(`${baseUrl}/question`);
        } else {
          props?.onSuccess();
        }
      }
    }
  ].filter(item => !item?.hidden);
  if (typeof children === 'function') {
    return children({
      list: actionList
    });
  }
  return (
    <ButtonGroup
      list={actionList}
      more={
        props?.type === 'link' ? (
          <Button type="link">
            <Icon type="gengduo2" size={16} />
          </Button>
        ) : undefined
      }
    />
  );
});
export default Actions;

// 文件: src/components/Common/Question/Actions/AddDigitalTask.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { Button, App } from 'antd';
import DigitalTaskFormInner from '../../Digital/DigitalTaskFormInner';


AI智能面试题目生成系统 V1.0                                                    第 59 页
--------------------------------------------------------------------------------
const AddDigitalTask = createWithRemoteLoader({
  modules: ['components-core:FormInfo', 'components-core:Global@usePreset']
})(({ remoteModules, id, questionIds, data, onSuccess, isClient, ...props }) => {
  const [FormInfo, usePreset] = remoteModules;
  const { useFormModal } = FormInfo;
  const formModal = useFormModal();
  const { apis, ajax } = usePreset();
  const { message } = App.useApp();
  return (
    <Button
      {...props}
      onClick={() => {
        formModal({
          title: '添加数字人任务',
          size: 'small',
          autoClose: true,
          children: <DigitalTaskFormInner language={data.language} hasProbe={data.hasProbe} isClient={isClient} />,
          formProps: {
            onSubmit: async formData => {
              //先取消该问题的其他任务
              const { data: resData } = await ajax(
                Object.assign({}, isClient ? apis.clientAdmin.question.generateDigital : apis.question.generateDigital, {
                  data: {
                    ...formData,
                    questionIds: questionIds || [id]
                  }
                })
              );
              if (resData.code !== 0) {
                return false;
              }
              // 处理后端返回的结果
              if (resData.data && resData.data.success === false) {
                // 所有题目都已有视频的情况
                message.warning(resData.data.message);
                return false;
              } else if (resData.data && resData.data.success === true) {
                // 成功创建任务的情况
                message.success(resData.data.message);
                onSuccess && onSuccess();
              } else {
                // 兼容旧的返回格式
                message.success('成功创建数字人任务');
                onSuccess && onSuccess();
              }
            }
          }
        });
      }}
    />


AI智能面试题目生成系统 V1.0                                                    第 60 页
--------------------------------------------------------------------------------
  );
});
export default AddDigitalTask;

// 文件: src/components/Common/Question/Actions/AddProbeTask.js
import { createWithRemoteLoader } from '@kne/remote-loader';
import { Button, App } from 'antd';
import DigitalTaskFormInner from '../../Digital/DigitalTaskFormInner';
const AddProbeTask = createWithRemoteLoader({
  modules: ['components-core:Global@usePreset', 'components-core:FormInfo', 'components-core:FormInfo@useFormModal']
})(({ remoteModules, id, data, onSuccess, isClient, ...props }) => {
  const [usePreset, FormInfo, useFormModal] = remoteModules;
  const { apis, ajax } = usePreset();
  const { message } = App.useApp();
  const { fields } = FormInfo;
  const { InputNumber } = fields;
  const formModal = useFormModal();
  return (
    <Button
      {...props}
      onClick={async () => {
        formModal({
          title: '添加追问问题任务',
          autoClose: true,
          size: 'small',
          formProps: {
            onSubmit: async formData => {
              const { data: resData } = await ajax(
                Object.assign({}, isClient ? apis.clientAdmin.question.generateProbe : apis.question.generateProbe, {
                  data: {
                    questionIds: [id],
                    ...formData
                  }
                })
              );
              if (resData.code !== 0) {
                return;
              }
              message.success('添加追问问题生成任务成功');
              onSuccess && onSuccess();
            }
          },
          children: <FormInfo column={1} list={[<InputNumber name="probeQuestionsNumber" label="生成数量" max={10} rule="REQ" precision={0} />]} />
        });
      }}
    />
  );
});
export default AddProbeTask;


